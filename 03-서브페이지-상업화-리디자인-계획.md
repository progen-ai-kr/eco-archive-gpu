# ECHO ARCHIVE 서브페이지 5종 상업화 리디자인 계획

> 근거 자료: `1차 미팅 정리.md`, `클라이언트 요청 기반 웹사이트 제작 방안.md`, `01-echo-archive-interactive-site-prd.md`
> 대상: `about.html`(1) · `products.html`(2) · `product.html`(3) · `portfolio.html`(4) · `contact.html`(5)
> 제약: 프레임워크·번들러·npm 빌드 없음. `worker.js`·`catalog.js` 보안 함수·관리자 파일은 보호 대상.

---

## 0. 브랜드 정체성 요약 (모든 디자인 판단의 기준)

| 축 | 내용 | 웹에서 지켜야 할 것 |
|---|---|---|
| 정의 | 현실과 비현실, **그 경계에서 비롯된 기록을 보관하는 패션 아카이브** | 페이지는 '상품 진열'이 아니라 '기록 열람'처럼 읽혀야 한다 |
| 슬로건 | Every Identity Echoes | 하나의 카피가 전 페이지에서 반복·변주된다 |
| 가치 | 개성 · 재미 · 경험 | 정적인 카탈로그 금지. 최소 1개 이상 발견형 인터랙션 |
| 타깃 | 서브컬처 취향의 10~20대 여성 | 밀도 높고 빠른 그리드, 모바일 우선 |
| 시즌 | 2026 F/W — 음양(Yin & Yang) | 흑/백 대비, 대칭↔비대칭, 부드러움↔날카로움 |
| 심볼 | 대시 `—` 가 원·점·파형으로 변주 | 이 변주 자체를 UI 부품으로 만든다 |
| 구성 단위 | Episode / Signal / Capsule | '상품'이라는 단어 대신 아카이브 언어 |
| 제작 | 대전 중촌동 장인 소량 제작 | 정직한 생산 서사를 About의 클라이맥스로 |

### 현재 상태 진단 (왜 아직 상업용으로 안 보이는가)

홈은 완성도가 높지만 **서브페이지 5개가 전부 "제목 한 줄 + 그리드 한 덩어리"** 다.

1. `products.html` / `portfolio.html` / `contact.html`은 `.page-head` + `.section` 두 블록이 전부다. 스크롤 리듬이 없다.
2. 홈에서 만든 세계관(CRT·신호·음양)이 서브페이지 진입 순간 끊긴다. 공통 시각 장치가 헤더/푸터뿐이다.
3. 브랜드가 가진 **분류 체계(Episode 번호, 신호 코드, 음/양 구분)가 화면에 전혀 노출되지 않는다.** 상업 브랜드 사이트의 신뢰감은 대부분 이 메타데이터 밀도에서 나온다.
4. 타이포 위계가 약하다. 명품/서브컬처 브랜드는 히어로 타이포가 화면을 지배한다.
5. CTA가 없다. 어느 페이지에서도 "다음에 무엇을 하라"는 지시가 없다.

---

## 1. 일관된 경험을 만드는 공통 시스템 — "SIGNAL SHEET"

페이지별로 따로 디자인하면 5개가 따로 논다. **먼저 4개의 공용 부품을 만들고, 5개 페이지는 그 부품의 배치만 달리한다.** 이것이 일관성의 유일한 보장책이다.

### A. 채널 헤드 (`.channel-head`) — 모든 페이지의 첫 화면

모든 서브페이지가 **동일한 해부구조**로 열린다. 값만 바뀐다.

```
CH.02 — SIG_LOOKS                      [음/양 인디케이터]
▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁
A L L   L O O K S
수집된 신호를 열고 각 CAPSULE의 세계를 탐색한다
                                    2026 F/W — YIN & YANG
```

- 좌상단 **신호 코드**(모노스페이스, 작게): `CH.01 SIG_ABOUT` / `CH.02 SIG_LOOKS` / `CH.03 SIG_EPISODE` / `CH.04 SIG_RECORD` / `CH.05 SIG_OPEN`
- 거대 타이틀: `clamp(3rem, 9vw, 8.5rem)`, 자간 `.08em`, 두 줄 허용
- 우하단 시즌 스탬프 고정
- 배경: 아주 옅은 스캔라인 + 진입 시 0.4s 노이즈 페이드 (`prefers-reduced-motion`에서 생략)

→ 사용자가 어느 페이지로 가든 **같은 방송국의 다른 채널**로 느낀다. 이것이 이 계획의 핵심 장치다.

### B. 음양 룰 (`.yy-rule`) — 섹션 구분선

브랜드 심볼(대시 `—`)의 UI 변주. 절반은 검정, 절반은 흰색, 경계에 작은 원 노드.

```
━━━━━━━━━━━━━━●───────────────
```

모든 섹션 사이에 들어간다. 스크롤 시 노드가 좌→우로 미끄러진다(IntersectionObserver, CSS transform만). 심볼 가이드의 "원·점·파형 변주"를 실제 기능으로 만든 것이라 클라이언트 설득이 쉽다.

### C. 아카이브 카드 (`.archive-card`) — 목록의 최소 단위

기존 `.glass-panel .glass-corners`를 확장한다. products / portfolio / about 값 카드가 **모두 이 하나를 공유**한다.

- 좌상단: 신호 코드 `EP.01 / CAT_404`
- 우상단: 음/양 배지 (● 양 / ○ 음) — `products.json`의 `label`/`category`에서 파생
- 이미지: 4:5 고정 비율, `object-fit: cover`
- hover: 이미지 1.04 스케일 + 스캔라인 1회 통과 + 하단에서 `VIEW EPISODE →` 슬라이드 업
- 키보드 포커스에도 동일 상태 적용 (접근성)

### D. 신호 전환 (`signal-transition.js`) — 페이지 간 이동

이미 홈에 구현되어 있다. **이걸 서브페이지 전체의 내부 링크로 확장**한다. 페이지가 바뀔 때마다 짧은 노이즈 컷이 들어가면 5개 페이지가 하나의 '채널 시스템'으로 묶인다. `prefers-reduced-motion`이면 즉시 이동.

### E. 네비게이션을 '채널 목록'으로

현재 `.nav-menu`는 평범한 텍스트 링크다. 각 항목 앞에 `CH.01`~`CH.05`를 붙이고, 현재 페이지에만 점멸하는 전원등(`.tv-led` 재사용)을 켠다. 홈 TV월과 같은 부품을 쓰므로 코드도 재사용되고 세계관도 이어진다.

---

## 2. 데스크톱 페이지별 계획

> 각 항목의 "레퍼런스"는 **무엇을 가져올지**를 특정한 것이다. 화면 캡처나 코드를 복제하지 않고 구조 패턴만 참고한다(제작 방안 §5 "사용하지 않을 방향" 준수).

---

### 1번 — `about.html` · CH.01 SIG_ARCHIVE

**목표**: 방문자가 "이 브랜드는 왜 존재하는가"를 90초 안에 이해하고, 취향이 맞으면 확신을 갖게 한다.

**레퍼런스와 가져올 패턴**
| 레퍼런스 | 가져올 것 |
|---|---|
| Maison Margiela | 넘버링 라벨 시스템 — 각 섹션에 `01 / 02 / 03` 인덱스를 붙여 '문서' 느낌 |
| Ader Error | 개념 문장 하나를 화면 전체 크기로 띄우는 챕터 브레이크 |
| Our Legacy / Studio Nicholson | 제작 공정 사진 + 좁은 칼럼 본문의 에디토리얼 리듬 |
| Rick Owens | 흑↔백 반전 섹션으로 만드는 극단적 대비 |

**데스크톱 구성 (위→아래)**
1. **채널 헤드** — `EVERY / IDENTITY / ECHOES` 3줄 대형 타이포
2. **정의 챕터** (흰 바탕) — 좌 40% 얇은 본문 / 우 60% 여백. "현실과 비현실, 그 경계에서 비롯된 기록" 한 문장을 대형으로
3. **음양 룰**
4. **반전 챕터** (검정 바탕, 흰 글자) — 시즌 테마 음양 선언. 페이지에서 유일하게 색이 뒤집히는 구간. 중앙에 음양 심볼(홈 TV의 인라인 SVG 재사용)이 스크롤에 따라 회전
5. **핵심 가치 3분할** — 개성 / 재미 / 경험. `.archive-card` 재사용, 각 카드에 심볼 변주(원·점·파형) 하나씩
6. **장인 제작 섹션** — 좌측 sticky 헤드라인 "중촌동에서 완성되는 기록", 우측 스크롤되는 공정 스텝 4개. 브랜드의 진정성 클라이맥스
7. **CTA 밴드** — `ALL LOOKS 보러 가기` + `2026 F/W 아카이브`

**핵심 판단**: About은 지금 이미 4개 섹션이 있어 구조가 제일 낫다. **완전 재작성이 아니라 챕터 넘버링·반전 섹션·sticky 공정만 추가**하는 게 투자 대비 효과가 크다.

---

### 2번 — `products.html` · CH.02 SIG_LOOKS

**목표**: 제품이 4개뿐이어도 빈약해 보이지 않게 한다. **밀도 대신 위계로 승부.**

**레퍼런스와 가져올 패턴**
| 레퍼런스 | 가져올 것 |
|---|---|
| SSENSE | 좌측 고정 필터 레일 + 우측 그리드. 상단이 아니라 **옆에** 필터를 두는 배치 |
| A-COLD-WALL* | 그리드 사이에 들어가는 대형 타이포 배너(제품이 적을 때 화면을 채우는 정석) |
| Ader Error | 카드 hover 시 이미지가 교체되며 메타데이터가 드러나는 방식 |
| Aimé Leon Dore | 첫 카드만 2칸 차지하는 비대칭 그리드 |

**데스크톱 구성**
1. **채널 헤드**
2. **필터 레일 (좌측 고정, 200px)** — 전체 / ● 양(YANG) / ○ 음(YIN) / Episode별. **JS 클라이언트 필터**(`products.json` 이미 전량 로드 중이라 서버 왕복 없음). 데이터 구조는 건드리지 않고 `label`/`category` 문자열에서 파생
3. **비대칭 그리드 (우측)** — 4열 기준, **첫 제품은 2×2 점유**. 제품이 늘면 규칙적 4열로 자연 확장
4. 그리드 6칸마다 **음양 룰 + 시즌 문장 배너** 1개 삽입 → 제품이 적어도 스크롤이 허전하지 않다
5. **하단 아카이브 안내** — "기록은 계속 수집됩니다" + 포트폴리오/문의 링크

**빈 상태 개선**: 현재는 텍스트 한 줄(`CAPSULE ARCHIVE SYNCING`)뿐. 스캔라인이 흐르는 CSS 전용 플레이스홀더 카드 4장으로 교체한다. 상업 사이트는 로딩·빈 상태에서 티가 난다.

---

### 3번 — `product.html` · CH.03 SIG_EPISODE

**목표**: 관리자가 쓴 자유 서식 본문이 **어떤 내용이 들어와도 무너지지 않고 에디토리얼로 보이게** 한다.

> ⚠ 이 페이지는 `[data-product-carousel]` / `[data-carousel-slide|previous|next|index|current]` / `#purchaseDialog` / `[data-purchase-dialog]` / `.purchase-dialog-close` 이름을 **반드시 유지**해야 한다. 인라인 스크립트가 널 체크 없이 이 선택자에 의존한다. 리스타일만 허용, 이름 변경 금지.

**레퍼런스와 가져올 패턴**
| 레퍼런스 | 가져올 것 |
|---|---|
| Acne Studios | 좌측 이미지 스택(세로로 계속 쌓임) + 우측 sticky 구매 정보 레일 |
| Our Legacy | 소재·사이즈·제작 정보를 접이식 아코디언으로 정리 |
| Bottega Veneta | 제품 이미지 사이에 룩북 컷을 끼워 넣는 리듬 |
| Aimé Leon Dore | 제품 하단의 '이야기' 블록 — 상품이 아니라 서사로 마무리 |

**데스크톱 구성**
1. **에피소드 헤드** — 제품명 대형 타이포 + `EP.0X / SIG_CODE` + 음/양 배지
2. **2단 레이아웃**: 좌 62% 이미지 스택(기존 캐러셀은 유지하되 데스크톱에서는 세로 스택으로도 전개) / 우 38% **sticky 구매 레일** — 가격, 한 줄 소개, 사이즈·소재 아코디언, 구매/문의 CTA
3. **음양 룰**
4. **관리자 본문(`sanitizeRichText` 출력)** — 이 구간만 폭을 720px로 좁혀 읽기 리듬 확보. `rich-content.css`의 `[data-align]`·`[data-brand-gallery]` 등 기존 선택자 계약을 그대로 존중하며 여백·타이포만 조정
5. **다음 에피소드** — 이전/다음 제품 카드 2장 (`.archive-card` 재사용). 이탈 방지의 표준 장치

---

### 4번 — `portfolio.html` · CH.04 SIG_RECORD

**목표**: 지금 가장 비어 있는 페이지. **영상이 아직 없어도 완성돼 보이는 구조**를 만든다(제작 방안 §8).

**레퍼런스와 가져올 패턴**
| 레퍼런스 | 가져올 것 |
|---|---|
| NOWNESS | 영상 썸네일 그리드 — 재생 버튼과 러닝타임 메타 |
| Dazed / i-D | 서브컬처 에디토리얼의 불규칙 매거진 그리드(대·중·소 혼합) |
| Bottega Veneta 캠페인 아카이브 | 시즌별 세로 아카이브 인덱스 + 연도 앵커 |
| Cargo Collective 계열 | 캡션이 이미지 옆에 작게 붙는 '기록물' 톤 |

**데스크톱 구성**
1. **채널 헤드** — `RECORDED / SIGNALS`
2. **시즌 인덱스 바** (sticky) — `2026 F/W` 등 시즌 앵커. 지금은 하나여도 구조를 미리 만든다
3. **매거진 그리드** — 3열 기준, 아이템이 `large / wide / normal` 3가지 폭을 가진다. `portfolio.json` 항목 순서에 따라 패턴을 순환 적용해 **데이터를 안 바꾸고도 불규칙한 리듬**을 만든다
4. **영상 슬롯 예약** — 16:9 영역에 포스터 이미지 + 중앙 재생 버튼. 영상 URL이 비면 "SIGNAL PENDING" CSS 플레이스홀더. 외주 영상 도착 시 URL만 넣으면 끝
5. **음양 룰 + 무드보드 스트립** — 가로 스크롤 이미지 띠
6. **CTA** — 문의 페이지로

**주의**: `portfolio.json`은 워커가 GitHub raw에서 **라이브 프록시**하므로 관리자 수정이 즉시 반영된다. 레이아웃은 항목 수가 1개든 20개든 깨지지 않아야 한다.

---

### 5번 — `contact.html` · CH.05 SIG_OPEN

**목표**: 폼이 없는 문의 페이지(현재 이메일·SNS 링크만)를 **약점이 아니라 콘셉트**로 만든다. "채널을 여는" 화면.

**레퍼런스와 가져올 패턴**
| 레퍼런스 | 가져올 것 |
|---|---|
| Locomotive / Bureau Borsche 등 스튜디오 사이트 | 이메일 주소 자체를 화면 최대 타이포로 쓰는 히어로 |
| Off-White 고객지원 | 문의 유형별 채널 분기(구매/협업/미디어)를 명시 |
| Acne Studios 스토어 정보 | 위치·운영 정보를 정갈한 데이터 테이블로 |

**데스크톱 구성**
1. **채널 헤드** — `OPEN / CHANNEL`
2. **대형 mailto 히어로** — `echo_archive@gmail.com`을 `clamp(2rem, 6vw, 5rem)`로. hover 시 밑줄이 좌→우로 그어짐. 클릭 = 메일 앱
3. **채널 3분할** — `PURCHASE 구매·재고` / `COLLAB 협업·제안` / `PRESS 미디어`. 각각 어디로 연락할지와 **예상 회신 시간**을 명시. 상업적 신뢰의 핵심
4. **음양 룰**
5. **정보 테이블** — 브랜드명, 대표(방려명), 제작 지역(대전 중촌동), Instagram, 운영 시간
6. **푸터 위 "돌아가기"** — 홈 TV월로 돌아가는 링크(신호 전환 사용)

**하지 않을 것**: 백엔드가 없으므로 동작하지 않는 문의 폼을 만들지 않는다. 가짜 폼은 상업적 신뢰를 오히려 깎는다.

---

## 3. 모바일 계획 (360~430px 우선)

데스크톱을 축소하는 방식이 아니라, **같은 부품을 모바일 규칙으로 다시 조립**한다. 타깃이 10~20대 여성이므로 실사용 대부분이 모바일이다.

### 전역 규칙
| 항목 | 규칙 |
|---|---|
| 채널 헤드 | 타이틀 `clamp(2.2rem, 12vw, 3.4rem)`, 신호 코드는 유지(작게), 시즌 스탬프는 타이틀 아래로 내림 |
| 네비 | 햄버거 → **전체화면 채널 목록 오버레이**. `CH.01`~`CH.05`가 세로로, 현재 채널에 전원등 점등. TV 채널 전환의 은유가 모바일에서 가장 잘 산다 |
| 탭 타깃 | 최소 44×44px |
| 음양 룰 | 두께·여백 축소, 노드 애니메이션 유지 |
| 모션 | 노이즈 전환 지속시간 40% 단축. `prefers-reduced-motion` 완전 준수 |
| 가로 스크롤 | 어떤 페이지도 body 가로 스크롤 금지. 넓은 요소는 자체 `overflow-x:auto` 컨테이너 안에서만 |

### 페이지별
1. **About** — 반전(검정) 챕터는 **전체 화면 높이 100dvh**로. 모바일에서 색 반전은 강력한 챕터 구분이 된다. sticky 공정 섹션은 세로 스텝 리스트로 전환(sticky 해제)
2. **Products** — 좌측 필터 레일 → **상단 가로 스크롤 칩**(전체/양/음). 그리드는 **1열 풀블리드**(카드 좌우 여백 12px). 첫 제품 2×2 규칙은 해제. 배너는 유지하되 높이 축소
3. **Product** — sticky 구매 레일 → **하단 고정 구매 바**(가격 + CTA, `env(safe-area-inset-bottom)` 반영). 이미지는 기존 캐러셀 그대로 사용하되 도트 인디케이터를 크게. 아코디언은 전부 접힌 상태로 시작
4. **Portfolio** — 매거진 그리드 → **1열 세로 스택 + 스크롤 스냅**. 영상 슬롯은 16:9 유지, `playsinline`·`muted`·자동재생 없음. 무드보드 스트립은 가로 스와이프 유지(모바일에서 오히려 자연스러움)
5. **Contact** — 대형 mailto는 폰트만 축소하고 **줄바꿈 허용**(`word-break`). 채널 3분할은 세로 카드 3장. Instagram·메일은 각각 독립 탭 타깃

### 태블릿(768~1024px)
Products 그리드 2열, Product는 이미지 위 / 정보 아래 1단, Portfolio 2열. 별도 디자인 없이 브레이크포인트만 추가.

---

## 4. 실행 순서 (PR 단위)

각 PR은 브랜치 → `gh pr create` → 머지 큐 → 로컬 main 동기화 순서를 따른다. **한 PR에 한 덩어리만.**

| # | 범위 | 파일 | 왜 이 순서인가 |
|---|---|---|---|
| 1 | **SIGNAL SHEET 공통 시스템** — `.channel-head`, `.yy-rule`, `.archive-card`, 채널형 네비 | `style.css`, 5개 HTML의 헤더/헤드 영역 | 부품이 먼저 있어야 나머지 4개 PR이 짧아진다. **여기서 일관성이 결정된다** |
| 2 | **Products (2번)** | `products.html`, `style.css` | 제품 페이지가 상업성 체감이 가장 크다. 카드 시스템 검증도 겸함 |
| 3 | **Product 상세 (3번)** | `product.html`, `style.css` | 2번에서 넘어오는 흐름. DOM 계약 이름 유지 필수 |
| 4 | **About (4번 작업, 1번 페이지)** | `about.html`, `style.css` | 기존 구조가 있어 증분 작업 |
| 5 | **Portfolio + Contact (4·5번)** | `portfolio.html`, `contact.html`, `style.css` | 둘 다 구조가 단순해 한 PR로 묶어도 안전 |
| 6 | **모바일 통합 점검** | `style.css` | 각 PR에서 모바일을 같이 만들되, 마지막에 5개를 실기기 폭에서 한 번에 검수 |

각 PR마다: `node --test tests/*.test.mjs` 전부 통과 + Chrome으로 데스크톱/모바일 폭 육안 확인 + 콘솔 에러 0.

### 테스트 계약 추가 제안
`tests/` 에 `subpage-contract.test.mjs`를 새로 만든다.
- 5개 페이지 모두 `.channel-head`와 신호 코드를 가진다
- 5개 페이지 모두 동일한 헤더·푸터 마크업을 가진다
- `product.html`의 캐러셀·구매 다이얼로그 선택자 이름이 존재한다(리디자인 중 이름 드리프트 방지)
- 각 페이지의 h1이 정확히 1개다

---

## 5. 손대지 않을 것

- `worker.js`, 관리자 파일, `catalog.js`의 보안 함수(`sanitizeRichText`, `safe*Url`)
- `products.json` / `portfolio.json`의 **데이터 구조** — 필터·배지는 기존 필드에서 파생만 한다
- 홈 `index.html`의 TV월 좌표계(1586×992 캔버스 기준 퍼센트)
- `product.html`의 DOM 속성 계약 이름
- `rich-content.css` 선택자와 `sanitizeRichText` 허용목록의 대응 관계 — 한쪽만 바꾸면 렌더링이 조용히 깨진다

## 6. 확인이 필요한 사항

- 제품별 **음/양 분류 기준** — 필터와 배지의 근거. 현재 `products.json`에 명시 필드가 없어 `label`/`category` 문자열에서 파생할 예정
- Episode 번호 체계(EP.01~) 확정 여부
- 문의 유형별 **회신 시간** 표기 값
- 포트폴리오 영상 납기 — 영상 슬롯 플레이스홀더를 얼마나 오래 유지할지
