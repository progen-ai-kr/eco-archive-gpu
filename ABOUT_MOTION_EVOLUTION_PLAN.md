# About 애니메이션 발전 계획

작성일: 2026-09-15  
개정일: 2026-09-16 — 단계별 구현·기기별 동작·글자 흐림 판정 보완

상태: 계획에 따른 구현 및 브라우저 검증 완료. 단계별 결과는 §10에 기록.
참고: [아난타 한국어 공식 사이트](https://www.anantagame.com/kr/)  
비교 대상: 현재 `about.html`, `style.css`의 About 블록, `scripts/about.js`

## 1. 제안의 핵심

**사진이 먼저 들어오고, 제목이 옆에서 따라오며, 충분히 읽은 뒤 다음 기록으로 넘어가는 About을 만든다.**

현재의 인트로 + MEMORY → TRANSLATE → EVERYDAY → EPISODE & ARCHIVE → LOCAL CRAFT 순서는 유지한다. 각각에 다른 움직임을 부여해 기억·재해석·일상·기록·제작이라는 뜻을 전달한다.

가장 큰 변화는 세 가지다.

1. 사진과 글을 하나의 덩어리로 움직이던 방식을 분리한다.
2. 제목은 줄 단위로 옆에서 등장한다. **글자 흐림 기능은 유지하되 선명해지는 범위를 넓히고, 다시 흐려지는 시점을 화면 가장자리로 늦춘다.**
3. EPISODE & ARCHIVE 한 장면에 세로 스크롤로 진행되는 가로 사진 띠를 넣는다.

흑백·아이스·크롬, 기존 로고와 서체를 유지한다. 기록 번호·얇은 선·사진 캡션을 움직임의 연결 장치로 쓴다.

## 2. 실제 사이트 비교에서 확인한 것

아난타의 첫 화면, 대형 타이포 전환, 캐릭터 소개, 가로 사진 구간, 지점 소개를 실제 브라우저에서 스크롤하며 관찰했다. 아래는 관찰 결과이며, 뒤의 수치와 장면별 움직임은 ECHO ARCHIVE를 위한 제안이다. 아난타의 내부 구현 방식이나 정확한 애니메이션 시간을 확인한 것은 아니다.

| 항목 | 아난타에서 관찰한 화면 | 현재 About | 발전 방향 |
|---|---|---|---|
| 첫 인상 | 큰 이미지에서 윤곽 타이포와 별도 문구가 있는 화면으로 전환 | 인트로 타이포가 처음부터 정지 상태로 표시 | 제목 세 줄의 방향과 등장 순서 설계 |
| 구성의 변화 | 큰 캐릭터 장면, 사진 묶음, 이미지·본문 분할 화면으로 구성 변화 | 다섯 장면이 대부분 같은 사진 왼쪽·글 오른쪽 배치 | 교대 배치와 가로 아카이브 장면으로 리듬 변화 |
| 가로 움직임 | 아래로 스크롤하면 사진 묶음이 오른쪽에서 왼쪽으로 이동 | 모든 장면이 세로로 연결 | EPISODE에 한정해 가로 사진 전환 적용 |
| 화면의 층 | 이미지·큰 제목·설명·기록 표시가 다른 위치에 구성 | 사진과 본문 전체에 같은 흐림·축소 적용 | 사진, 제목, 설명, 보조 기록을 별도 대상으로 분리 |
| 장면 경계 | 큰 화면 전환과 사진 띠가 연결 | 이전 장면과 다음 장면이 동시에 흐려지고 가운데 여백이 크게 보이는 구간 존재 | 이전 내용을 읽을 수 있는 동안 다음 사진이 진입하도록 조정 |

관찰에서 가져올 원리는 ‘스크롤 방향과 화면 속 이동 방향을 다르게 설계하기’와 ‘장면의 구성 자체를 바꾸기’다. 캐릭터·배경·문구·색상은 브랜드 자산으로 사용하지 않는다.

## 3. 움직임의 공통 규칙

### 3.1 등장 → 읽기 → 전환

한 장면의 스크롤 진행을 아래처럼 설계한다. 비율은 사진과 제목의 등장 순서를 위한 첫 시안의 시작값이다. **글자 흐림은 이 장면 진행률로 판정하지 않고, §3.3의 실제 화면 안 위치로 별도 판정한다.** 긴 모바일 장면에서 사진만 보고 본문까지 흐리게 만드는 문제를 피하기 위해서다.

| 구간 | 화면에서 일어나는 일 | 확인 기준 |
|---|---|---|
| 진입 0~25% | 사진이 옆에서 들어오고 프레임이 열림 | 사진의 이동 방향을 느낄 수 있고 얼굴이 가려지지 않음 |
| 제목 15~35% | 기록 번호 → 제목의 첫 줄 → 둘째 줄 → 설명 순서로 등장 | 제목이 사진과 한꺼번에 튀어나오지 않음 |
| 읽기 35~80% | 등장 이동이 끝나고 본문 읽기에 집중 | 화면의 읽기 범위에 있는 문장은 장면 진행률과 무관하게 선명함 |
| 연결 80~100% | 다음 사진이 들어오고 현재 장면은 자연스럽게 화면 밖으로 이동 | 읽기 범위를 벗어난 글부터 흐려지며, 다음 장면 번호가 켜졌다는 이유로 이전 글이 흐려지지 않음 |

스크롤 진행에 연결할 효과와 시간에 연결할 효과를 구분한다.

- 사진 이동·가로 아카이브: 스크롤을 멈추면 그 위치에서 멈춘다.
- 제목·본문 등장: 진입을 감지하면 약 0.45~0.7초 안에 완료한다. 문장이 반쯤 나온 상태로 멈춰 읽기 어려워지는 것을 피한다.
- 영문 대형 제목은 단어·줄 단위, 한국어 제목은 의미가 있는 줄 단위로 나눈다. 글자 하나씩 날리는 효과는 사용하지 않는다.
- 위로 돌아갈 때 사진 전환은 자연스럽게 되감기고 글도 다시 선명해진다. 이미 읽은 글도 화면 가장자리를 벗어나면 다시 흐려질 수 있다. 등장 위치 이동의 재생 여부와 흐림 상태는 분리한다.

### 3.2 효과의 강도

- 사진의 좌우 이동: 데스크톱 기준 약 80~160px에서 시작한다.
- 제목의 좌우 이동: 약 40~72px에서 시작한다. 전체 화면을 가로질러 날아오게 하지 않는다.
- 줄별 시간차: 약 80~120ms. 둘째 줄까지 읽을 수 있게 되는 데 0.9초 이상 걸리지 않게 한다.
- 본문 이동: 아래에서 약 12~20px. 읽기 구간에서는 opacity 1, blur 0, transform 없음.
- 사진과 글자 모두 흐림 효과를 유지한다. 1차 시안은 기존 흐림 강도를 유지한 채 판정 범위부터 넓혀 효과를 비교한다. 본문을 계속 선명하게 고정하는 방식으로 대체하지 않는다.
- 반동·회전·번쩍임을 겹치지 않는다. 각 장면에는 대표 움직임 하나를 둔다.

### 3.3 핵심 보완 — 글자 흐림은 유지하고 판정을 여유롭게

#### 현재 문제가 생기는 이유

- 현재 CSS는 `.about-scene-inner` 전체에 같은 흐림을 적용한다. 사진과 제목·본문이 함께 흐려진다.
- CSS 경로에서 완전히 선명한 구간은 애니메이션 진행률 35~65%다. 이 숫자는 화면 높이의 35~65%를 의미하지 않는다.
- 대체 Observer 경로는 화면 높이의 중앙 30%에 장면이 들어왔는지를 본다. 장면이 중앙에서 벗어나면 아직 화면에 남은 글도 흐려질 수 있다.
- 장면 간 여백이 큰 곳에서는 이전 장면과 다음 장면이 동시에 비활성 상태가 된다.

수정의 우선순위는 **판정 범위 확장 → 장면 전체에서 문단 단위로 판정 분리 → 경계 왕복 시 상태 안정화**다. 흐림을 없애는 것은 목표가 아니다.

#### 첫 시안의 판정 수치

아래 비율은 **실제로 읽을 수 있는 화면 높이**를 기준으로 한다. 고정 진행 바·상단 메뉴가 가리는 높이와 하단 안전 여백을 제외하고, 남은 영역의 위를 0%, 아래를 100%로 둔다. 섹션 전체 길이의 비율과 구분한다.

| 항목 | 데스크톱: 900px 이상 | 모바일: 900px 미만 |
|---|---|---|
| 선명해지기 시작하는 범위 | 화면의 15~85%, 중앙 70% | 화면의 10~90%, 중앙 80% |
| 한 번 선명해진 후 유지하는 범위 | 화면의 10~90%, 중앙 80% | 화면의 5~95%, 중앙 90% |
| 다시 흐려지는 시점 | 해당 글 블록 전체가 유지 범위를 완전히 벗어났을 때 | 해당 글 블록 전체가 유지 범위를 완전히 벗어났을 때 |
| 선명해지는 전환 시간 | 약 160ms | 약 120ms |
| 다시 흐려지는 전환 시간 | 약 320ms | 약 280ms |
| 선명한 상태 | blur 0, opacity 1 | blur 0, opacity 1 |
| 최초 비교 시 흐림 최댓값 | 현재 경로의 14px·opacity .25 / 대체 경로 10px·.3 유지 | 현재 6px·opacity .45 유지 |

숫자는 확정된 검증 결과가 아니라 첫 시안의 시작값이다. **먼저 범위만 바꾸어 비교한다.** 가장자리 글자가 여전히 지나치게 흐리면 2차 조정에서 글자 강도를 별도로 낮춘다. 사진 강도까지 동시에 바꿔 원인을 구분하기 어렵게 만들지 않는다.

#### 판정 예시와 경계 처리

1. 아래에서 들어오는 제목이나 문단의 일부가 선명해지는 범위에 닿으면, 해당 제목/문단 전체가 선명해진다. 문단 중심점이 중앙까지 올 때까지 기다리지 않는다.
2. 한 번 선명해진 문단은 더 넓은 유지 범위에 조금이라도 남아 있으면 선명함을 유지한다. 긴 문단은 윗부분이 화면 밖으로 나갔어도 나머지를 계속 읽을 수 있다.
3. 유지 범위를 완전히 떠났을 때만 흐림이 돌아온다. 들어올 때와 나갈 때 경계를 다르게 두어 작은 스크롤 왕복으로 깜빡이는 것을 막는다.
4. 이전 장면의 마지막 문단과 다음 장면의 제목이 모두 읽기 범위에 있으면 **둘 다 선명하게 표시**한다. 진행 번호는 하나여도 선명한 장면은 둘 이상일 수 있다.
5. `is-current`는 진행 번호 전용, `is-focused`는 각 글·사진의 흐림 전용으로 구분한다. 두 상태를 서로의 숨김 조건으로 사용하지 않는다.
6. 제목, 본문 문단, 목록 항목, 사진 캡션을 의미 단위로 판정한다. 글자 하나씩 판정하거나 한 장면 전체를 한 번에 흐리게 하지 않는다.
7. 사진이 고정되어 있는 동안에도 오른쪽 글은 자기 위치로 판정한다. 사진의 위치 때문에 글이 먼저 흐려지지 않는다.
8. 위로 스크롤할 때도 같은 규칙을 적용한다. 흐려진 글이 읽기 범위에 다시 들어오면 다시 선명해진다.

예를 들어 가려지는 영역을 제외한 화면 높이가 800px이면, 데스크톱은 위에서 120~680px에서 선명해지고 80~720px까지 유지한다. 모바일은 80~720px에서 선명해지고 40~760px까지 유지한다.

#### 구현 방식

- 움직이지 않는 판정용 바깥 요소 안에 등장 이동용 요소와 흐림용 요소를 둔다. 글자가 이동하면서 자신의 판정 위치까지 바꾸지 않게 한다.
- 선명 진입 범위와 선명 유지 범위를 담당하는 Observer를 각각 만들고 여러 글 블록을 함께 관찰한다. 들어옴/나감 결과를 종합한 후 상태를 갱신해 두 Observer의 호출 순서로 깜빡이지 않게 한다.
- `rootMargin`의 %는 너비 기준이므로 실제 화면 높이로 px 값을 계산한다. 15%, 10%를 문자열 그대로 넣지 않는다.
- 흐림은 CSS 전환으로 연결하고, 연속적인 사진 이동·가로 띠 이동은 별도의 스크롤 애니메이션으로 처리한다. CSS 스크롤 지원 여부와 관계없이 글자가 선명해지는 범위는 같게 맞춘다.
- 1차 비교에서 기존 흐림 강도를 유지하고, 판정이 안정된 다음 브라우저별 차이와 글자 강도를 조정한다.
- 화면 크기·회전·모바일 주소창 높이 변경 시 읽기 범위를 재계산한다. 매 스크롤마다 글의 위치를 읽는 반복문은 만들지 않는다.

### 3.4 장면 사이의 여백과 판정은 따로 조정

범위를 넓혀도 원래 비어 있는 여백에 글이 생기지는 않는다. 판정을 바꾼 다음에도 경계가 지나치게 비어 보이는지 별도로 확인한다.

- 일반 장면의 연결 여백은 데스크톱 약 48~80px, 모바일 약 32~48px를 첫 시안의 시작값으로 잡는다. 앞 장면의 하단 패딩 + 구분선 + 다음 장면의 상단 패딩을 합쳐 비교한다.
- 섹션에 큰 최소 높이가 지정되어 생기는 내부 빈 공간도 함께 점검한다. 기존 `105svh`를 모든 장면에 일괄 유지하지 않고 고정 사진에 필요한 높이와 실제 본문 길이로 조절한다.
- 위·아래 글이 이미 읽기 범위 안에 있는데 흐린 상태라면 판정 문제다. 글 자체가 화면 밖에 있어서 비어 보이면 레이아웃 문제다. 두 문제를 구분해 수정한다.
- 아카이브의 가로 전환에 필요한 스크롤 거리는 일반 장면 여백과 별도로 계산한다.

## 4. 장면별 연출안

### 4.1 인트로 — 문장이 조립되는 첫 화면

목표: 큰 타이포가 브랜드의 시작을 알리게 한다.

1. `EVERY`는 왼쪽에서 제자리로 들어온다.
2. `IDENTITY`는 짧은 시간차로 오른쪽에서 왼쪽으로 들어온다.
3. `ECHOES.`는 아래에서 올라오며 전체 문장을 완성한다.
4. 한줄 소개와 `SCROLL TO READ`가 뒤따른다.

각 줄의 표시 영역을 고정해, 글자가 해당 영역 안에서 드러나게 한다. 본문 배치는 움직이지 않으므로 페이지 전체가 밀리지 않는다. 타이포 연출은 첫 방문의 짧은 시작 효과이며 사용자가 스크롤하면 즉시 다음 내용에 접근할 수 있어야 한다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | EVERY: x -64→0px, IDENTITY: x +64→0px, ECHOES.: y +24→0px. 줄마다 90ms 간격, 각 이동 520ms를 시작값으로 사용 |
| 모바일 | 좌우 이동 ±20px, 아래 이동 12px, 줄 간격 60ms, 각 이동 360ms. 문구를 실제 폭에 맞춰 줄바꿈하고 가로 넘침 방지 |
| 공통 구현 | h1 안에 의미 단위 줄 wrapper를 추가하고, 안쪽 span의 transform만 이동. 흐림은 §3.3의 별도 요소가 담당. 읽기 범위로 바로 진입하거나 해시로 건너뛰면 대기 지연 생략 |

**확인 기준:** h1은 하나, 세 줄의 순서가 명확함, 한국어 소개는 0.9초 안에 표시, 빠르게 스크롤해도 빈 화면 없음.

### 4.2 MEMORY — 왼쪽에서 사진이 발견된다

목표: 기억 속 사진 한 장을 꺼내는 느낌.

1. 왼쪽 사진이 바깥쪽에서 안쪽으로 약 120px 이동한다.
2. 사진을 보여 주는 영역이 왼쪽부터 열리며 장면이 드러난다.
3. 오른쪽 제목 두 줄은 각각 오른쪽에서 왼쪽으로 약 56px 이동한다.
4. 설명이 짧게 나타난 뒤 정지한다. 사진 프레임은 설명을 읽는 동안 머문다.
5. `REC_001`과 얇은 선을 마지막에 드러낸다.

사진 진입과 제목 진입은 겹치되 시작을 다르게 한다. 사진보다 글이 먼저 사라지는 전환은 피한다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | 사진 x -120→0px, 제목 x +56→0px. 사진 진입 후 약 100ms 뒤 제목 시작, 줄별 90ms 차이. 제목 이동 520ms, 본문 y +16→0px/320ms |
| 모바일 | 사진 x -24→0px, 제목 x +16→0px, 본문 y +12→0px. 각 요소가 실제 화면에 들어올 때 따로 시작하고 사진 완료를 기다려 본문을 숨기지 않음. sticky 해제 |
| 공통 구현 | 사진의 고정 wrapper와 안쪽 이동·마스크를 분리. 제목·문단별 Observer를 붙이고 선명 진입 범위 70%/80% 적용. 제목과 본문은 등장 이동과 흐림 판정을 독립적으로 처리 |

**확인 기준:** 사진과 제목이 별도로 들어오는 것을 체감, 얼굴·칼라 보존, 설명을 읽는 구간에서 문장 전체가 선명함.

### 4.3 TRANSLATE — 재해석 과정을 순서대로 보여 준다

목표: 네 단계가 연결되어 옷이 되는 과정을 시각화한다.

1. 데스크톱에서는 글을 왼쪽, 사진을 오른쪽으로 교대 배치한다.
2. 제목은 왼쪽에서 오른쪽으로, 사진은 오른쪽에서 왼쪽으로 이동해 각자의 자리에 멈춘다.
3. `분위기 → 색과 실루엣 → 디테일 → 일상복`의 얇은 연결선이 순서대로 길어진다.
4. 해당 단계의 번호와 문구가 차례로 강조되며, 앞서 등장한 단계도 읽을 수 있게 남는다.

단계별 강조는 기존 텍스트로 구현한다. 현재 사진 하나가 실제로 다른 의상으로 변하는 것처럼 보이는 연출은 하지 않는다. 배치만 교대하며 보조기기가 읽는 내용 순서는 일관되게 유지한다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | 글 왼쪽·사진 오른쪽. 제목 x -48→0px, 사진 x +100→0px. 4개 단계는 가로로 놓고 연결선을 왼쪽부터 확장 |
| 모바일 | 사진 위·글 아래. 사진 x +24→0px, 제목 x -16→0px. 과정은 1열의 세로 목록으로 구성하고 연결선도 위→아래로 확장 |
| 공통 구현 | 기존 ol/li를 유지하고 각 항목에 기록 번호와 선을 연결. 항목이 보이는 순서에 따라 선의 scaleX/scaleY와 번호 강조를 적용. 강조되지 않은 문구도 읽기 범위에 있으면 선명하게 표시 |

**확인 기준:** 네 단계가 빠짐없이 표시, 현재 단계가 구분됨, 사진과 긴 한국어 제목이 겹치지 않음.

### 4.4 EVERYDAY — 읽는 속도로 가볍게 흘러간다

목표: 앞의 연출에서 잠시 쉬며 일상이라는 메시지를 읽게 한다.

1. 사진은 왼쪽, 설명은 오른쪽으로 돌아온다.
2. 사진의 위치에만 작은 속도 차이를 준다. 스크롤하는 본문에 비해 사진이 조금 늦게 따라오는 정도로 제한한다.
3. 제목은 아래에서 한 번 등장하고 이후 정지한다.
4. 추가 고정 구간 없이 자연스러운 세로 흐름을 유지한다.

인물과 배경은 현재 한 장의 사진에 합쳐져 있다. 인물만 따로 움직이는 효과는 현재 자산으로 가정하지 않는다. 기존 크롭에서 손·소매 일부가 잘린 점을 고려해 추가 확대와 강한 이미지 내부 이동을 피한다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | 사진 바깥 프레임 전체에 y +20→-20px 정도의 작은 속도 차이. 제목은 y +20→0px/420ms. 별도 화면 고정 구간 없음 |
| 모바일 | 사진의 지속 이동은 생략하고 y +12→0px/320ms의 짧은 등장만 적용. 제목은 12px 이동, 사진과 본문 모두 자기 위치에서 초점 판정 |
| 공통 구현 | 지원 브라우저에서만 사진의 연속 이동 적용. 이미지를 확대하거나 프레임 안에서 움직여 추가로 크롭하지 않음. 본문 흐림은 공통 판정 규칙 그대로 유지 |

**확인 기준:** 앞 장면보다 차분한 리듬, 얼굴이 프레임 바깥으로 밀리지 않음, 읽기 범위의 본문이 선명하고 가장자리를 벗어나면 흐림 복귀.

### 4.5 EPISODE & ARCHIVE — 세로 스크롤로 넘기는 가로 사진 기록

목표: 이 페이지에서 가장 기억에 남는 장면. 아카이브를 펼쳐 보는 경험을 만든다.

1. 제목과 짧은 설명이 먼저 자리 잡는다.
2. 데스크톱에서 이 장면의 화면을 잠시 고정한다. 문서의 세로 스크롤은 계속된다.
3. 스크롤 진행에 따라 사진 띠가 오른쪽에서 왼쪽으로 이동한다.
4. 사진 캡션과 작은 기록 번호가 사진 아래에 함께 붙어 움직인다.
5. 마지막 사진이 충분히 보이면 화면 고정이 끝나고 LOCAL CRAFT로 이어진다.

첫 시안은 기존 `scene-*.webp` 4장을 다시 활용한다. 앞에서는 각 개념을 설명한 사진을 이곳에서는 하나의 기록 묶음으로 보여 준다. 4장을 서로 다른 Episode라고 표기하지 않고 `FRAME 01~04`처럼 사진의 순서만 표시한다. 실제 영상·촬영 비하인드가 없는 상태에서 재생 버튼이나 가짜 기록을 추가하지 않는다.

가로 띠의 이동 거리는 실제 콘텐츠 너비에서 화면의 표시 너비를 뺀 값으로 잡는다. 모든 사진이 지나간 뒤에도 계속 스크롤해야 하는 빈 구간이 생기지 않게 한다. 첫 시안의 장면 길이는 약 2.5~3.5개 화면 높이 안에서 맞추고, 사진 수·표시 너비에 따라 줄인다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | 위쪽 제목·설명과 아래쪽 사진 띠로 구성. 외부 section은 세로 진행 거리, 내부 stage는 sticky, 내부 track은 x 0→-이동거리로 이동. 끝 15% 정도는 마지막 사진을 읽는 구간으로 확보 |
| 모바일 | sticky와 가로 트랙 변환을 해제하고 같은 사진 4장을 세로로 배치. 카드별 x -20→0px/360ms의 짧은 진입 후, 화면 위·아래 위치에 따른 흐림 유지 |
| 공통 구현 | 이미지 준비·폰트 준비·폭 변경 후 이동 거리 산출. 트랙이 표시 폭보다 짧으면 가로 전환 자체를 생략. 제목·캡션은 각각 별도 초점 대상으로 두고 실제 표시 위치로 판정 |

데스크톱의 가로 띠에서는 사진 카드의 좌우 노출 여부도 별도로 본다. 중앙 세로 읽기 범위에 있는 제목은 선명하게 유지한다. 카드나 캡션의 실제 표시 영역이 화면에 걸쳐 있는 동안, 트랙 바깥의 위치를 기준으로 잘못 흐려지지 않게 한다. 가로 이동 중에는 좌우 위치에 대한 별도의 흐림 효과를 추가하지 않고, 화면 위·아래 읽기 범위 판정만 적용한다.

**확인 기준:** 아래로 스크롤했을 때 사진이 확실히 왼쪽으로 이동, 마지막 사진 전체와 캡션 확인 가능, 휠을 가로로 조작할 필요 없음, 위로 돌아가면 역방향으로 연결, 장면이 끝나면 정상적인 세로 스크롤 복귀.

### 4.6 LOCAL CRAFT — 제작 과정이 쌓이며 마무리된다

목표: 움직임이 가라앉고 실제 제작 철학에 시선이 머물게 한다.

1. 로고 패널이 왼쪽에서 짧게 들어와 멈춘다.
2. 오른쪽 제목이 한 번 등장한다.
3. `스케치 → 소재·패턴 협의 → 소량 제작 → 검수와 기록`이 위에서 아래 순서로 강조된다.
4. 마지막에는 제품 페이지 버튼을 안정적으로 표시한다.

로고를 계속 회전시키거나 CTA가 스크롤에 따라 사라지게 하지 않는다. 실제 공방 사진이 없으므로 현재 로고 패널을 사용한다.

| 구분 | 효과와 구현 |
|---|---|
| 데스크톱 | 로고 x -48→0px/480ms, 제목 x +40→0px/480ms. 과정은 위→아래 순으로 강조하고 버튼은 마지막 항목과 같은 읽기 흐름에 배치 |
| 모바일 | 로고 y +16→0px/320ms, 제목 x +16→0px/360ms. 목록 항목마다 실제 진입 시 강조하며, 전체 목록이 들어올 때까지 기다리지 않음 |
| 공통 구현 | 기존 로고와 ol/li 재사용. 목록 항목의 강조와 글자 초점을 분리. CTA는 항상 누를 수 있게 두고, 키보드 포커스가 있는 요소는 흐림 판정보다 선명 상태를 우선 적용 |

**확인 기준:** 네 단계와 버튼이 전부 접근 가능, 마지막 본문도 읽기 범위에서는 선명하고 화면 가장자리에서 흐림 복귀, 버튼에 키보드 포커스를 주면 정지하고 선명함.

## 5. 모바일과 접근성

900px 미만에서는 기존처럼 사진 위·글 아래의 세로 배치를 사용한다.

- 데스크톱·모바일의 효과는 §4 각 장면의 기기별 표를 따른다. 데스크톱을 완성한 뒤 일괄 축소하지 않고 각 단계에서 두 화면을 함께 확인한다.
- 글자 흐림은 두 기기 모두 유지한다. 모바일의 선명 진입 범위는 중앙 80%, 선명 유지 범위는 중앙 90%로 더 넓힌다.
- 사진 이동은 16~32px, 제목 이동은 12~24px로 줄인다.
- 제목은 한 줄씩 길게 기다리게 하지 않고 짧은 순차 등장만 적용한다.
- EPISODE의 고정·자동 가로 이동은 해제하고 사진을 세로로 펼친다. 핵심 사진을 보려면 별도로 가로 스와이프해야 하는 구조를 만들지 않는다.
- 화면 높이가 짧거나 글자 확대 때문에 고정 영역에 내용이 들어가지 않으면 데스크톱에서도 세로 흐름으로 전환한다.
- JS 없음, Observer 없음, 동작 줄이기 설정에서는 모든 내용을 처음부터 선명하게 표시한다.
- 동작 줄이기에서는 줄 등장, 사진 이동, 고정, 가로 전환을 모두 해제한다. 페이지 도중 설정을 바꿔도 현재 내용을 잃지 않는다.
- 제목을 span으로 나눠도 읽는 순서와 접근성 이름은 한 문장으로 유지한다. 장식 복제 텍스트는 보조기기에서 제외한다.
- 빠른 스크롤, 장면 번호 링크, 키보드 포커스로 건너뛰면 해당 장면을 바로 읽을 수 있게 한다.
- 모바일 상단 진행 바와 기존 메뉴가 겹치지 않는 현재 동작을 보존한다.

## 6. 구현 방향과 기존 명세와의 관계

`ABOUT_REDESIGN_SPEC.md`의 브랜드 구성·보호 파일·순수 HTML/CSS/JS·접근성·900px 레이아웃 원칙을 이어받는다. §4.4(c)의 글자·사진 흐림 기능과 역스크롤 시 초점 전환은 유지한다. 변경 대상은 전체 장면에 함께 적용하는 방식과 좁은 선명 판정 범위다. 이를 ‘의미 단위의 개별 판정 + 넓은 선명 범위 + 여유 있는 이탈 경계’로 발전시킨다. 원본 지시서 자체는 수정하지 않는다.

### 변경 예정 파일

| 파일 | 예정 변경 |
|---|---|
| `about.html` | 제목 줄을 감싸는 요소, 사진 표시 영역, 가로 아카이브 트랙 등 About 내부 구조 |
| `style.css` | `.about-page` 아래 요소별 효과와 모바일·동작 줄이기 분기 |
| `scripts/about.js` | 요소별 등장·초점 상태, 선명 진입/유지 Observer, 진행 번호, 화면 조건에 따른 효과 활성화 |

기존 사진 4장과 로고로 첫 시안을 만든다. 이미지 재생성·재크롭은 이 계획의 기본 범위에 포함하지 않는다.

### 구현 원칙

1. 기본 HTML/CSS는 정적인 완성 페이지다. 기능 지원을 확인한 후에만 효과를 켠다.
2. 제목 등 짧은 등장 효과는 IntersectionObserver와 CSS 전환으로 처리한다.
3. 사진의 연속 이동·가로 아카이브는 지원되는 CSS 스크롤 애니메이션에 연결한다. 미지원 브라우저는 짧은 등장 효과와 세로 배치로 대체한다.
4. 순수 IntersectionObserver로 연속적인 가로 이동까지 재현할 수 있다고 가정하지 않는다. 기능 차이를 계획에 포함한다.
5. 기존 금지 사항인 매 스크롤마다 위치를 읽는 반복문과 scroll 리스너는 추가하지 않는다. 필요한 크기 계산은 최초 배치·이미지 준비·크기 변경 때만 한다.
6. `.about-scene-inner` 전체에 걸린 흐림을 요소별 흐림 대상으로 이전한다. **글자 흐림 기능은 유지**하고 부모·자식의 filter/opacity가 중복 적용되지 않게 한다. 등장용 transform과 초점용 filter/opacity의 담당 요소도 분리한다.
7. sticky로 고정하는 바깥 요소와 움직이는 안쪽 요소를 분리한다. 움직이는 요소가 자신의 진행도 측정 기준까지 바꾸지 않게 한다.
8. 이동은 transform, 명암은 opacity 중심으로 처리한다. 사진을 여는 마스크는 사진 프레임 한 곳에 제한하고 실기기 성능을 확인한다.
9. 공유 메뉴·푸터·제품·문의 CSS와 운영 데이터는 유지한다. 기존 장면 ID, h1 1개, nav 속성 계약, 채널 헤드, 구분선 계약도 유지한다.

## 7. 단계별 작업 순서와 통과 기준

각 단계는 데스크톱·모바일을 함께 검증한다. 아래 표는 통과 기준이며, 실제 수행 결과와 검증의 범위는 §10을 따른다.

| 단계 | 넣을 효과·구현 작업 | 데스크톱 확인 | 모바일 확인 | 통과 기준 |
|---|---|---|---|---|
| 1. 기준 확보 | 기존 경계 구간을 기록하고 사진·제목·문단의 판정 요소 분리 | 중앙 판정 밖에 남은 문장 위치 기록 | 사진이 지난 뒤 늦게 나타나는 본문 위치 기록 | 원인과 기준 화면을 확보하고 공용 코드 변경 없음 |
| 2. 초점 범위 개선 | 기존 강도를 유지하고 진입/유지 Observer 분리, 진행 번호와 초점 상태 분리 | 중앙 70% 진입·80% 유지 | 중앙 80% 진입·90% 유지 | 읽기 범위의 글은 선명하며, 유지 범위를 나가면 글 흐림이 실제로 복귀 |
| 3. MEMORY 시안 | 사진 옆 진입 + 제목 줄별 진입 + 본문 짧은 이동 | -120px 사진·+56px 제목, 사진 고정 | -24px 사진·+16px 제목, 세로 배치 | 이동 중 판정 흔들림 없음, 빠른 스크롤에서도 읽기 가능 |
| 4. 인트로 | 제목 세 줄의 서로 다른 방향 등장 | ±64px·줄 간격 90ms | ±20px·줄 간격 60ms | 첫 화면 문구가 0.9초 내 표시, 너비 변경 시 잘림 없음 |
| 5. EPISODE | 기존 사진 4장의 가로 기록 띠 | 세로 스크롤에 가로 이동 연결, 마지막 사진에서 고정 해제 | 같은 DOM을 세로 카드로 표시, 카드별 짧은 등장 | 4장과 캡션 모두 접근 가능, 역방향 전환·짧은 화면 대체 정상 |
| 6. 나머지 장면 | TRANSLATE 교대 배치·공정 연결선, EVERYDAY 작은 이동, CRAFT 단계 강조 | 좌우 구성과 장면별 리듬 확인 | 사진 위·글 아래, 긴 목록 개별 판정 | 기기마다 효과가 과하지 않고 글 흐림은 넓은 공통 판정을 따름 |
| 7. 경계 조율 | 연결 여백·두 장면 동시 초점·작은 스크롤 왕복 조정 | 모든 인접 장면 경계에서 중간 정지 | 동일 경계 + 주소창 변화·회전 | 읽기 범위의 이전/다음 글 모두 선명, 경계 10회 왕복 시 깜빡임 없음 |
| 8. 최종 확인 | 접근성·성능·기존 사이트 검사 | 1440px·900px·짧은 높이·확대 | 899px·390px·360px·터치 | §8 전체 통과 후 구현 완료 |

우선순위는 **글자 흐림 판정 개선 → MEMORY의 분리 등장 → 인트로 → 가로 아카이브 → 나머지 장면 → 경계 검증**이다. 경계 가독성 문제를 해결한 상태에서 새 효과를 쌓는다.

## 8. 최종 검증 체크리스트

- [ ] 1440×900, 900px 경계, 899px, 390px, 360px, 높이 700px 이하 화면에서 레이아웃 확인.
- [ ] 200% 글자 확대에서 제목·본문·CTA가 잘리지 않음. 고정 영역이 부족하면 세로로 전환.
- [ ] 페이지 전체 가로 넘침 없음. EPISODE 트랙만 정해진 표시 영역 안에서 이동.
- [ ] 모든 본문이 읽기 구간에서 opacity 1, blur 0, 정지 상태.
- [ ] 데스크톱 15~85% 진입·10~90% 유지, 모바일 10~90% 진입·5~95% 유지가 실제 가용 화면 높이 기준으로 동작.
- [ ] 글자 흐림 기능 자체는 유지되어, 유지 범위를 완전히 벗어난 글은 다시 흐려지고 돌아오면 다시 선명해짐.
- [ ] 인트로→MEMORY→TRANSLATE→EVERYDAY→EPISODE→CRAFT→푸터의 모든 경계에서 잠시 멈춰 확인. 읽기 범위의 이전 문단과 다음 제목이 동시에 선명할 수 있음.
- [ ] 경계 부근을 약 10~20px씩 10회 왕복해도 불필요한 초점 깜빡임 없음. 긴 문단은 일부가 유지 범위에 남아 있으면 선명함.
- [ ] 진행 번호 변경이 이전 장면의 글자를 흐리게 만드는 조건으로 사용되지 않음.
- [ ] 중간 스크롤 멈춤, 빠른 통과, 역스크롤, 직접 해시 진입에서 빈 화면 없음.
- [ ] 진행 링크 5개, 메뉴, 제품 CTA의 마우스·키보드 접근 정상.
- [ ] JS 차단·Observer 미지원·CSS 스크롤 애니메이션 미지원에서 모든 콘텐츠 접근 가능.
- [ ] 동작 줄이기를 처음부터 켜거나 도중에 바꿔도 이동·고정·흐림 없음.
- [ ] 실제 모바일에서 사진 마스크와 이동 때문에 눈에 띄는 끊김 없음. 문제가 있으면 마스크를 먼저 단순화.
- [ ] 기존 테스트 전체와 JS 문법·정적 사이트 검사 통과, 브라우저 오류 0건.
- [ ] products / product / portfolio / contact 레이아웃과 공용 CSS 보존.
- [ ] 보호 파일·운영 데이터·기존 별도 작업의 변경이 이번 구현에 섞이지 않음.

## 9. 완료됐을 때의 사용자 경험

첫 화면에서 문장이 조립되고, 스크롤하면 왼쪽에서 기억의 사진이 나타난다. 다음 장면에서는 사진과 글의 자리가 바뀌며 재해석 과정이 순서대로 강조된다. 일상 장면에서 잠시 편하게 읽고, 아카이브에서는 아래로 스크롤하며 가로로 펼쳐진 사진 기록을 넘긴다. 마지막에는 움직임이 가라앉고 중촌동의 제작 과정과 제품으로 이어지는 버튼이 남는다.

이 흐름을 목표로 삼는다. 화려한 효과의 개수보다 **각 장면에서 무엇을 먼저 보고, 어디서 읽고, 어떻게 다음으로 넘어가는지**가 명확해야 한다.

## 10. 구현 결과 — 2026-09-16

### 단계별 확인

| 단계 | 구현 결과 | 확인 결과 |
|---|---|---|
| 1. 기준 확보 | 기존 전체 장면 흐림을 확인하고, 제목·문단·사진·캡션을 62개 의미 단위로 분리 | 1440px/390px 기준 화면 확보. 공용 메뉴·운영 데이터 수정 없음 |
| 2. 초점 범위 | 두 Observer로 70% 진입/80% 유지(데스크톱), 80% 진입/90% 유지(모바일). 기존 최대 흐림 강도 유지 | 읽기 범위 진입 → 선명 → 유지 범위 이탈 → 흐림 → 역스크롤 선명 확인. 두 화면에서 유지 경계 10회 왕복 통과 |
| 3. MEMORY | 사진 -120px, 제목 +56px, 줄별 등장. 모바일 -24px/+16px. 지원 브라우저의 사진은 스크롤 연결 | 사진·제목 분리와 얼굴 보존 확인. 감지 wrapper에 이동 효과 없음 |
| 4. 인트로 | 제목 세 줄을 좌/우/아래에서 짧게 등장 | h1 1개 유지, 좁은 화면 잘림 없음. 직접 장면 이동 시 등장 대기 생략 |
| 5. EPISODE | 기존 사진 4장, 세로 스크롤 기반 가로 이동. 모바일 세로 카드 | 정방향/역방향 이동, 마지막 사진·캡션 노출 확인. 좌우 이동과 초점 판정을 분리하는 높이 기준선 사용 |
| 6. 나머지 장면 | TRANSLATE 교대 배치·과정 연결선, EVERYDAY 작은 사진 이동, CRAFT 제작 단계 표시 | 데스크톱 및 모바일 화면 확인. CTA는 항상 접근 가능 |
| 7. 경계 조율 | 장면 사이 기본 여백 데스크톱 80px/모바일 48px, 일괄 105svh 최소 높이 제거 | 전체 페이지를 스크롤하며 읽기 범위의 의미 단위를 검사. 6개 화면 조건에서 초점 누락 0건 |
| 8. 최종 확인 | 접근성·기능 지원별 대체·사이트 회귀 검사 | 아래 검증 결과 참고 |

### 구현 중 조정한 사항

- 아카이브의 고정 시작 위치를 가용 화면의 선명 진입 범위 안쪽으로 옮겨 제목·설명이 고정 상태에서 흐려지지 않게 했다.
- 가로 사진 띠는 실제 넘치는 거리만 이동하고, 끝 15%에 마지막 사진을 유지한다. 장면 길이를 고정된 화면 개수로 강제하지 않는다.
- 가로 고정 영역의 전체 내용이 화면에 들어가지 않거나 높이가 720px 미만이면 세로 흐름으로 전환한다. 데스크톱 대체 화면은 사진을 2열, 900px 미만은 1열로 보여 준다.
- 모바일의 옆 진입으로 생기던 2px 가로 넘침은 About 본문에만 가로 잘림 영역을 적용해 제거했다. 세로 sticky 동작은 유지한다.
- 글자 200% 확대에 해당하는 제목·본문·설명·기록 글씨 확대에서도 고정이 해제되는지 확인했다.

### 검증 기록

- Chromium에서 1440×900, 900×900, 899×900, 390×844, 360×800, 1440×650 검사: About 가로 넘침 0px, 읽기 범위의 초점 누락 0건.
- 재실행 가능한 브라우저 검사: `tests/about-motion.browser.cjs`. 정적 서버 실행 후 설치된 Playwright 경로를 `PLAYWRIGHT_MODULE`, 서버 주소를 `ABOUT_BASE_URL`에 지정한다. 사이트에 런타임 패키지는 추가하지 않았다.
- 직접 장면 링크 5개, CTA 키보드 포커스, 모바일 메뉴, 동작 줄이기 초기/도중 전환, JS 없음, Observer 없음, CSS 스크롤 타임라인 없음 모두 통과. 브라우저 JavaScript 오류 0건.
- 기존 자동 검사 38개 및 저장소 정적 사이트 검증 통과. 커밋 후 보호 파일과 최신 main 조합 검사는 PR에서 다시 실행한다.
- Products/Product/Portfolio/Contact: 데스크톱·모바일 가로 넘침 0px, 메뉴·푸터 유지, 브라우저 오류 0건. 홈의 390px 화면에서 측정된 72px 넘침은 기존 `origin/main` CSS에서도 동일하여 이번 About 변경과 무관함을 확인했다.
- 모바일 검증은 브라우저 화면 크기와 기능 설정 기반이다. 실제 휴대전화의 GPU 성능, Safari 실기기 스크롤 및 주소창 동작은 직접 측정하지 않았다.

### 변경 범위

`about.html`, `scripts/about.js`, `style.css`의 About 전용 규칙, 이 계획서, About 브라우저 검사만 포함한다. 별도 진행 중인 제품 작업과 분리한 작업 폴더에서 마무리했다.
