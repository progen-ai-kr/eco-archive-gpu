# About 애니메이션 발전 계획

작성일: 2026-09-15  
상태: 비교 분석과 구현 계획. 이 문서 작성 단계에서는 페이지 코드를 변경하지 않음.  
참고: [아난타 한국어 공식 사이트](https://www.anantagame.com/kr/)  
비교 대상: 현재 `about.html`, `style.css`의 About 블록, `scripts/about.js`

## 1. 제안의 핵심

**사진이 먼저 들어오고, 제목이 옆에서 따라오며, 충분히 읽은 뒤 다음 기록으로 넘어가는 About을 만든다.**

현재의 인트로 + MEMORY → TRANSLATE → EVERYDAY → EPISODE & ARCHIVE → LOCAL CRAFT 순서는 유지한다. 각각에 다른 움직임을 부여해 기억·재해석·일상·기록·제작이라는 뜻을 전달한다.

가장 큰 변화는 세 가지다.

1. 사진과 글을 하나의 덩어리로 움직이던 방식을 분리한다.
2. 제목은 줄 단위로 옆에서 등장하고, 본문은 짧게 등장한 뒤 선명하게 유지한다.
3. EPISODE & ARCHIVE 한 장면에 세로 스크롤로 진행되는 가로 사진 띠를 넣는다.

흑백·아이스·크롬, 기존 로고와 서체를 유지한다. 기록 번호·얇은 선·사진 캡션을 움직임의 연결 장치로 쓴다.

## 2. 실제 사이트 비교에서 확인한 것

아난타의 첫 화면, 대형 타이포 전환, 캐릭터 소개, 가로 사진 구간, 지점 소개를 실제 브라우저에서 스크롤하며 관찰했다. 아래는 관찰 결과이며, 뒤의 수치와 장면별 움직임은 ECHO ARCHIVE를 위한 제안이다. 아난타의 내부 구현 방식이나 정확한 애니메이션 시간을 확인한 것은 아니다.

| 항목 | 아난타에서 관찰한 화면 | 현재 About | 발전 방향 |
|---|---|---|---|
| 첫 인상 | 큰 이미지에서 윤곽 타이포와 별도 문구가 있는 화면으로 전환 | 인트로 타이포가 처음부터 정지 상태로 표시 | 제목 세 줄의 방향과 등장 순서 설계 |
| 구성의 변화 | 큰 캐릭터 장면, 사진 묶음, 이미지·본문 분할 화면으로 구성 변화 | 다섯 장면이 대부분 같은 사진 왼쪽·글 오른쪽 배치 | 교대 배치와 가로 아카이브 장면으로 리듬 변화 |
| 가로 움직임 | 아래로 스크롤하면 사진 묶음이 오른쪽에서 왼쪽으로 이동 | 모든 장면이 세로로 연결 | EPISODE에 한정해 가로 사진 전환 적용 |
| 화면의 층 | 이미지·큰 제목·설명·기록 표시가 다른 위치에 구성 | 사진과 본문 전체에 같은 흐림·축소 적용 | 사진, 제목, 설명, 보조 기록을 별도 대상으로 분리 |
| 장면 경계 | 큰 화면 전환과 사진 띠가 연결 | 이전 장면과 다음 장면이 동시에 흐려지고 가운데 여백이 크게 보이는 구간 존재 | 이전 내용을 읽을 수 있는 동안 다음 사진이 진입하도록 조정 |

관찰에서 가져올 원리는 ‘스크롤 방향과 화면 속 이동 방향을 다르게 설계하기’와 ‘장면의 구성 자체를 바꾸기’다. 캐릭터·배경·문구·색상은 브랜드 자산으로 사용하지 않는다.

## 3. 움직임의 공통 규칙

### 3.1 등장 → 읽기 → 전환

한 장면의 스크롤 진행을 아래처럼 설계한다. 비율은 첫 시안의 시작값이며, 본문 길이에 맞춰 조정한다.

| 구간 | 화면에서 일어나는 일 | 확인 기준 |
|---|---|---|
| 진입 0~25% | 사진이 옆에서 들어오고 프레임이 열림 | 사진의 이동 방향을 느낄 수 있고 얼굴이 가려지지 않음 |
| 제목 15~35% | 기록 번호 → 제목의 첫 줄 → 둘째 줄 → 설명 순서로 등장 | 제목이 사진과 한꺼번에 튀어나오지 않음 |
| 읽기 35~80% | 본문은 정지하고 선명하게 유지 | 문장을 읽는 중에 흐림이나 이동이 다시 시작하지 않음 |
| 연결 80~100% | 다음 사진이 들어올 준비를 하고 현재 장면은 자연스럽게 화면 밖으로 이동 | 이전·다음 본문이 동시에 읽히지 않는 공백을 만들지 않음 |

스크롤 진행에 연결할 효과와 시간에 연결할 효과를 구분한다.

- 사진 이동·가로 아카이브: 스크롤을 멈추면 그 위치에서 멈춘다.
- 제목·본문 등장: 진입을 감지하면 약 0.45~0.7초 안에 완료한다. 문장이 반쯤 나온 상태로 멈춰 읽기 어려워지는 것을 피한다.
- 영문 대형 제목은 단어·줄 단위, 한국어 제목은 의미가 있는 줄 단위로 나눈다. 글자 하나씩 날리는 효과는 사용하지 않는다.
- 위로 돌아갈 때 사진 전환은 자연스럽게 되감긴다. 이미 읽은 본문은 다시 숨기지 않는 것을 기본값으로 한다.

### 3.2 효과의 강도

- 사진의 좌우 이동: 데스크톱 기준 약 80~160px에서 시작한다.
- 제목의 좌우 이동: 약 40~72px에서 시작한다. 전체 화면을 가로질러 날아오게 하지 않는다.
- 줄별 시간차: 약 80~120ms. 둘째 줄까지 읽을 수 있게 되는 데 0.9초 이상 걸리지 않게 한다.
- 본문 이동: 아래에서 약 12~20px. 읽기 구간에서는 opacity 1, blur 0, transform 없음.
- 흐림은 사진 진입의 보조 효과로만 선택 적용한다. 본문 전체에 걸리는 기존 14px 흐림은 대체한다.
- 반동·회전·번쩍임을 겹치지 않는다. 각 장면에는 대표 움직임 하나를 둔다.

## 4. 장면별 연출안

### 4.1 인트로 — 문장이 조립되는 첫 화면

목표: 큰 타이포가 브랜드의 시작을 알리게 한다.

1. `EVERY`는 왼쪽에서 제자리로 들어온다.
2. `IDENTITY`는 짧은 시간차로 오른쪽에서 왼쪽으로 들어온다.
3. `ECHOES.`는 아래에서 올라오며 전체 문장을 완성한다.
4. 한줄 소개와 `SCROLL TO READ`가 뒤따른다.

각 줄의 표시 영역을 고정해, 글자가 해당 영역 안에서 드러나게 한다. 본문 배치는 움직이지 않으므로 페이지 전체가 밀리지 않는다. 타이포 연출은 첫 방문의 짧은 시작 효과이며 사용자가 스크롤하면 즉시 다음 내용에 접근할 수 있어야 한다.

**확인 기준:** h1은 하나, 세 줄의 순서가 명확함, 한국어 소개는 0.9초 안에 표시, 빠르게 스크롤해도 빈 화면 없음.

### 4.2 MEMORY — 왼쪽에서 사진이 발견된다

목표: 기억 속 사진 한 장을 꺼내는 느낌.

1. 왼쪽 사진이 바깥쪽에서 안쪽으로 약 120px 이동한다.
2. 사진을 보여 주는 영역이 왼쪽부터 열리며 장면이 드러난다.
3. 오른쪽 제목 두 줄은 각각 오른쪽에서 왼쪽으로 약 56px 이동한다.
4. 설명이 짧게 나타난 뒤 정지한다. 사진 프레임은 설명을 읽는 동안 머문다.
5. `REC_001`과 얇은 선을 마지막에 드러낸다.

사진 진입과 제목 진입은 겹치되 시작을 다르게 한다. 사진보다 글이 먼저 사라지는 전환은 피한다.

**확인 기준:** 사진과 제목이 별도로 들어오는 것을 체감, 얼굴·칼라 보존, 설명을 읽는 구간에서 문장 전체가 선명함.

### 4.3 TRANSLATE — 재해석 과정을 순서대로 보여 준다

목표: 네 단계가 연결되어 옷이 되는 과정을 시각화한다.

1. 데스크톱에서는 글을 왼쪽, 사진을 오른쪽으로 교대 배치한다.
2. 제목은 왼쪽에서 오른쪽으로, 사진은 오른쪽에서 왼쪽으로 이동해 각자의 자리에 멈춘다.
3. `분위기 → 색과 실루엣 → 디테일 → 일상복`의 얇은 연결선이 순서대로 길어진다.
4. 해당 단계의 번호와 문구가 차례로 강조되며, 앞서 등장한 단계도 읽을 수 있게 남는다.

단계별 강조는 기존 텍스트로 구현한다. 현재 사진 하나가 실제로 다른 의상으로 변하는 것처럼 보이는 연출은 하지 않는다. 배치만 교대하며 보조기기가 읽는 내용 순서는 일관되게 유지한다.

**확인 기준:** 네 단계가 빠짐없이 표시, 현재 단계가 구분됨, 사진과 긴 한국어 제목이 겹치지 않음.

### 4.4 EVERYDAY — 읽는 속도로 가볍게 흘러간다

목표: 앞의 연출에서 잠시 쉬며 일상이라는 메시지를 읽게 한다.

1. 사진은 왼쪽, 설명은 오른쪽으로 돌아온다.
2. 사진의 위치에만 작은 속도 차이를 준다. 스크롤하는 본문에 비해 사진이 조금 늦게 따라오는 정도로 제한한다.
3. 제목은 아래에서 한 번 등장하고 이후 정지한다.
4. 추가 고정 구간 없이 자연스러운 세로 흐름을 유지한다.

인물과 배경은 현재 한 장의 사진에 합쳐져 있다. 인물만 따로 움직이는 효과는 현재 자산으로 가정하지 않는다. 기존 크롭에서 손·소매 일부가 잘린 점을 고려해 추가 확대와 강한 이미지 내부 이동을 피한다.

**확인 기준:** 앞 장면보다 차분한 리듬, 얼굴이 프레임 바깥으로 밀리지 않음, 본문이 계속 선명함.

### 4.5 EPISODE & ARCHIVE — 세로 스크롤로 넘기는 가로 사진 기록

목표: 이 페이지에서 가장 기억에 남는 장면. 아카이브를 펼쳐 보는 경험을 만든다.

1. 제목과 짧은 설명이 먼저 자리 잡는다.
2. 데스크톱에서 이 장면의 화면을 잠시 고정한다. 문서의 세로 스크롤은 계속된다.
3. 스크롤 진행에 따라 사진 띠가 오른쪽에서 왼쪽으로 이동한다.
4. 사진 캡션과 작은 기록 번호가 사진 아래에 함께 붙어 움직인다.
5. 마지막 사진이 충분히 보이면 화면 고정이 끝나고 LOCAL CRAFT로 이어진다.

첫 시안은 기존 `scene-*.webp` 4장을 다시 활용한다. 앞에서는 각 개념을 설명한 사진을 이곳에서는 하나의 기록 묶음으로 보여 준다. 4장을 서로 다른 Episode라고 표기하지 않고 `FRAME 01~04`처럼 사진의 순서만 표시한다. 실제 영상·촬영 비하인드가 없는 상태에서 재생 버튼이나 가짜 기록을 추가하지 않는다.

가로 띠의 이동 거리는 실제 콘텐츠 너비에서 화면의 표시 너비를 뺀 값으로 잡는다. 모든 사진이 지나간 뒤에도 계속 스크롤해야 하는 빈 구간이 생기지 않게 한다. 첫 시안의 장면 길이는 약 2.5~3.5개 화면 높이 안에서 맞추고, 사진 수·표시 너비에 따라 줄인다.

**확인 기준:** 아래로 스크롤했을 때 사진이 확실히 왼쪽으로 이동, 마지막 사진 전체와 캡션 확인 가능, 휠을 가로로 조작할 필요 없음, 위로 돌아가면 역방향으로 연결, 장면이 끝나면 정상적인 세로 스크롤 복귀.

### 4.6 LOCAL CRAFT — 제작 과정이 쌓이며 마무리된다

목표: 움직임이 가라앉고 실제 제작 철학에 시선이 머물게 한다.

1. 로고 패널이 왼쪽에서 짧게 들어와 멈춘다.
2. 오른쪽 제목이 한 번 등장한다.
3. `스케치 → 소재·패턴 협의 → 소량 제작 → 검수와 기록`이 위에서 아래 순서로 강조된다.
4. 마지막에는 제품 페이지 버튼을 안정적으로 표시한다.

로고를 계속 회전시키거나 CTA가 스크롤에 따라 사라지게 하지 않는다. 실제 공방 사진이 없으므로 현재 로고 패널을 사용한다.

**확인 기준:** 네 단계와 버튼이 전부 접근 가능, 마지막 본문이 흐려지지 않음, 버튼에 키보드 포커스를 주면 정지하고 선명함.

## 5. 모바일과 접근성

900px 미만에서는 기존처럼 사진 위·글 아래의 세로 배치를 사용한다.

- 사진 이동은 16~32px, 제목 이동은 12~24px로 줄인다.
- 제목은 한 줄씩 길게 기다리게 하지 않고 짧은 순차 등장만 적용한다.
- EPISODE의 고정·자동 가로 이동은 해제하고 사진을 세로로 펼친다. 핵심 사진을 보려면 별도로 가로 스와이프해야 하는 구조를 만들지 않는다.
- 화면 높이가 짧거나 글자 확대 때문에 고정 영역에 내용이 들어가지 않으면 데스크톱에서도 세로 흐름으로 전환한다.
- JS 없음, Observer 없음, 동작 줄이기 설정에서는 모든 내용을 처음부터 선명하게 표시한다.
- 동작 줄이기에서는 줄 등장, 사진 이동, 고정, 가로 전환을 모두 해제한다. 페이지 도중 설정을 바꿔도 현재 내용을 잃지 않는다.
- 제목을 span으로 나눠도 읽는 순서와 접근성 이름은 한 문장으로 유지한다. 장식 복제 텍스트는 보조기기에서 제외한다.
- 빠른 스크롤, 장면 번호 링크, 키보드 포커스로 건너뛰면 해당 장면을 바로 읽을 수 있게 한다.
- 모바일 상단 진행 바와 기존 메뉴가 겹치지 않는 현재 동작을 보존한다.

## 6. 구현 방향과 기존 명세와의 관계

`ABOUT_REDESIGN_SPEC.md`의 브랜드 구성·보호 파일·순수 HTML/CSS/JS·접근성·900px 레이아웃 원칙을 이어받는다. 전체 장면에 흐림을 적용하는 §4.4(c)와 반복해서 본문을 숨기는 동작은 이 계획의 ‘요소별 등장 + 읽기 구간 유지’로 교체할 대상으로 명시한다. 원본 지시서 자체는 수정하지 않는다.

### 변경 예정 파일

| 파일 | 예정 변경 |
|---|---|
| `about.html` | 제목 줄을 감싸는 요소, 사진 표시 영역, 가로 아카이브 트랙 등 About 내부 구조 |
| `style.css` | `.about-page` 아래 요소별 효과와 모바일·동작 줄이기 분기 |
| `scripts/about.js` | 요소별 진입·읽음 상태, 진행 번호, 화면 조건에 따른 효과 활성화 |

기존 사진 4장과 로고로 첫 시안을 만든다. 이미지 재생성·재크롭은 이 계획의 기본 범위에 포함하지 않는다.

### 구현 원칙

1. 기본 HTML/CSS는 정적인 완성 페이지다. 기능 지원을 확인한 후에만 효과를 켠다.
2. 제목 등 짧은 등장 효과는 IntersectionObserver와 CSS 전환으로 처리한다.
3. 사진의 연속 이동·가로 아카이브는 지원되는 CSS 스크롤 애니메이션에 연결한다. 미지원 브라우저는 짧은 등장 효과와 세로 배치로 대체한다.
4. 순수 IntersectionObserver로 연속적인 가로 이동까지 재현할 수 있다고 가정하지 않는다. 기능 차이를 계획에 포함한다.
5. 기존 금지 사항인 매 스크롤마다 위치를 읽는 반복문과 scroll 리스너는 추가하지 않는다. 필요한 크기 계산은 최초 배치·이미지 준비·크기 변경 때만 한다.
6. `.about-scene-inner` 전체에 걸린 기존 animation/filter/transform을 먼저 해제한 후 새 효과를 적용한다. 기존 흐림과 새 등장 효과가 중첩되지 않게 한다.
7. sticky로 고정하는 바깥 요소와 움직이는 안쪽 요소를 분리한다. 움직이는 요소가 자신의 진행도 측정 기준까지 바꾸지 않게 한다.
8. 이동은 transform, 명암은 opacity 중심으로 처리한다. 사진을 여는 마스크는 사진 프레임 한 곳에 제한하고 실기기 성능을 확인한다.
9. 공유 메뉴·푸터·제품·문의 CSS와 운영 데이터는 유지한다. 기존 장면 ID, h1 1개, nav 속성 계약, 채널 헤드, 구분선 계약도 유지한다.

## 7. 단계별 작업 순서와 통과 기준

| 순서 | 작업 | 통과 기준 |
|---|---|---|
| 1 | 현재 화면 기준 저장, 요소별 역할과 등장 방향 지정 | 6개 장면의 대표 움직임과 정지 구간이 정리되고 공용 CSS 영향 범위 없음 |
| 2 | MEMORY 한 장면의 사진·제목·본문 분리 시안 | 사진 왼쪽 진입, 제목 옆 진입, 본문 읽기 구간을 1440px·390px에서 확인 |
| 3 | 인트로·TRANSLATE·EVERYDAY·LOCAL CRAFT에 리듬 적용 | 장면별 차이가 분명하고 줄별 지연이 길지 않으며 본문 동시 흐림 없음 |
| 4 | EPISODE 가로 아카이브 구현 | 첫 사진·마지막 사진 모두 완전히 볼 수 있음, 세로 복귀 정상, 모바일·미지원에서는 세로로 모두 표시 |
| 5 | 빠른 스크롤·역스크롤·직접 이동 보강 | 중간 진입과 키보드 이동에서 내용이 숨지 않음, 진행 번호가 현재 장면과 일치 |
| 6 | 접근성·성능·다른 페이지 회귀 검사 | 아래 최종 체크리스트 통과 후 완료 |

첫 구현 우선순위는 **MEMORY의 사진/제목 분리 → 인트로의 줄별 등장 → EPISODE의 가로 아카이브**다. 이 세 구간에서 방향과 속도를 먼저 맞춘 뒤 나머지 장면에 확장한다. 모든 장면에 다른 효과를 한꺼번에 적용하고 나중에 정리하는 방식은 피한다.

## 8. 최종 검증 체크리스트

- [ ] 1440×900, 900px 경계, 899px, 390px, 360px, 높이 700px 이하 화면에서 레이아웃 확인.
- [ ] 200% 글자 확대에서 제목·본문·CTA가 잘리지 않음. 고정 영역이 부족하면 세로로 전환.
- [ ] 페이지 전체 가로 넘침 없음. EPISODE 트랙만 정해진 표시 영역 안에서 이동.
- [ ] 모든 본문이 읽기 구간에서 opacity 1, blur 0, 정지 상태.
- [ ] 중간 스크롤 멈춤, 빠른 통과, 역스크롤, 직접 해시 진입에서 빈 화면 없음.
- [ ] 진행 링크 5개, 메뉴, 제품 CTA의 마우스·키보드 접근 정상.
- [ ] JS 차단·Observer 미지원·CSS 스크롤 애니메이션 미지원에서 모든 콘텐츠 접근 가능.
- [ ] 동작 줄이기를 처음부터 켜거나 도중에 바꿔도 이동·고정·흐림 없음.
- [ ] 실제 모바일에서 사진 마스크와 이동 때문에 눈에 띄는 끊김 없음. 문제가 있으면 마스크를 먼저 단순화.
- [ ] 기존 테스트 전체와 JS 문법·정적 사이트 검사 통과, 브라우저 오류 0건.
- [ ] products / product / portfolio / contact 레이아웃과 공용 CSS 보존.
- [ ] 보호 파일·운영 데이터·기존 별도 작업의 변경이 이번 구현에 섞이지 않음.

## 9. 완료됐을 때의 사용자 경험

첫 화면에서 문장이 조립되고, 스크롤하면 왼쪽에서 기억의 사진이 나타난다. 다음 장면에서는 사진과 글의 자리가 바뀌며 재해석 과정이 순서대로 강조된다. 일상 장면에서 잠시 편하게 읽고, 아카이브에서는 아래로 스크롤하며 가로로 펼쳐진 사진 기록을 넘긴다. 마지막에는 움직임이 가라앉고 중촌동의 제작 과정과 제품으로 이어지는 버튼이 남는다.

이 흐름을 목표로 삼는다. 화려한 효과의 개수보다 **각 장면에서 무엇을 먼저 보고, 어디서 읽고, 어떻게 다음으로 넘어가는지**가 명확해야 한다.
