# ECHO ARCHIVE 크롬 Y2K UI 구현 계획 및 Claude Code TDD 프롬프트

## 1. 이 문서의 목적

이 문서는 최종 승인된 크롬 Y2K 메인 시안을 ECHO ARCHIVE 정적 사이트에 구현하기 위한 실행 계획이다. 아래의 `Claude Code 전달용 마스터 프롬프트`를 Claude Code에 그대로 전달하면 된다.

최종 시안 원본:

`C:\Users\hb061\.codex\generated_images\01a080f8-beaf-7801-9248-56516d20e246\exec-a8ce2c49-5261-4cfb-bb93-2f49e5af0b5a.png`

기준 화면은 Chrome, DPR 1, `1584 × 1000px`이다. “시안과 동일”이라는 요구는 이 기준 화면에서 요소의 위치, 크기, 색, 재질, 여백, 시각적 무게가 오버레이 비교 시 거의 구분되지 않는 상태를 뜻한다. 작은 화면에서는 같은 조형 문법을 유지하면서 재배치한다.

## 2. 절대 조건

1. 공개 사이트 런타임은 순수 HTML, CSS, JavaScript만 사용한다.
2. React, Vue, Svelte, Tailwind, Bootstrap, 번들러, 새 런타임 프레임워크를 추가하지 않는다.
3. 최종 시안 전체를 한 장의 배경 이미지로 넣고 투명 클릭 영역만 얹는 방식은 금지한다. TV, 화면, 아이콘, 링크, 버튼, 제목은 실제 HTML 요소여야 한다.
4. 복잡한 날개·꼬리 장식은 투명 PNG 또는 SVG 자산을 사용할 수 있지만, 텍스트와 상호작용 요소는 이미지에 굽지 않는다.
5. 기존 `ProductCatalog`, `products.json`, `portfolio.json`, `/admin` 연결 구조를 유지한다.
6. 관리자·배포·저장·보안 파일과 운영 데이터는 수정하지 않는다.
7. 기존 작업트리의 변경 내용을 삭제하거나 덮어쓰지 않는다. 특히 `index.html`, `style.css`, `scripts/home.js`에 이미 있는 미커밋 변경부터 정확히 확인하고 그 의도를 새 구현에 포함한다.
8. 기능별로 RED → GREEN → REFACTOR 순서를 지키고, GREEN 검증 후 기능 단위로 커밋한다. 실패하는 상태는 커밋하지 않는다.
9. 데스크톱 기준 시안 일치와 모바일 사용성을 모두 성취 조건에 포함한다.
10. 화면 안에는 아이콘만 표시한다. TV 화면 내부에 글자, 숫자, 코드, 배지, 화살표, 캡션을 넣지 않는다.

## 3. 최종 시안의 디자인 문법

### 화면 전체

- 캔버스 비율: 16:10, 기준 1584 × 1000px.
- 배경: 순백색이 아니라 아주 옅은 아이스 그레이와 푸른 회색이 섞인 스튜디오 그라데이션.
- 바닥: 화면 아래 약 16% 영역에 유광 반사면이 있으며 TV와 아이콘이 매우 옅게 반사된다.
- 조명: 화면 상단 중앙에서 넓고 부드러운 확산광. 그림자는 차갑고 부드럽다.
- 전체 인상: Y2K, 크롬, 패션 에디토리얼, 수집형 오브제, 깨끗한 미래적 스튜디오.
- 화면에 카드, 설명 박스, 대시보드 형태를 추가하지 않는다.

### 상단 브랜드 영역

- 좌측 상단에 ECHO ARCHIVE 로고가 작고 넓게 배치된다.
- 로고 오른쪽에는 작은 별 모양 포인트가 있다.
- 로고 아래에는 자간이 넓은 작은 서브 문구가 있다. 실제 문구는 기존 브랜드 콘텐츠를 우선한다.
- 우측 상단에는 `ALL LOOKS`, `CONTACT` 두 링크가 모노스페이스 대문자로 배치된다.
- 헤더는 박스나 색면 없이 배경 위에 떠 있는 형태다.

### 메인 타이포그래피

- 왼쪽에는 `TOUCH`와 `THE`가 2줄로 배치된다.
- 오른쪽에는 `SCREEN!`이 한 줄로 배치된다.
- 검정색, 굵고 폭이 좁은 디스플레이 서체, 약간 오른쪽으로 기울어진 형태다.
- TV보다 앞으로 튀어나오지 않으며, 시안의 축소된 크기를 그대로 따른다.
- 기준 좌표의 시각적 범위:
  - 왼쪽 문구: 화면 폭 약 6–21%, 높이 약 20–37%.
  - 오른쪽 문구: 화면 폭 약 78–96%, 높이 약 22–33%.
- 모바일에서는 문구가 TV를 가리지 않도록 위쪽 제목으로 재배치한다.

### TV 월 배치

- TV는 정확히 5대다.
- 위쪽 2대, 아래쪽 3대의 중앙 대칭 구조다.
- 위쪽 행은 아래쪽 3대의 가운데에 정렬된다.
- TV 사이 간격은 거의 없고, 실제 오브제처럼 서로 맞닿아 있다.
- 기준 화면에서 TV 전체 조형물은 폭 약 70%, 높이 약 81%를 차지한다.
- 대략적인 외곽 기준:
  - 전체 TV 조형물: x 15.5–86%, y 2–84%.
  - 1번 TV: 위 왼쪽.
  - 2번 TV: 위 오른쪽.
  - 3번 TV: 아래 왼쪽.
  - 4번 TV: 아래 가운데.
  - 5번 TV: 아래 오른쪽.
- 위쪽 TV와 아래쪽 TV의 캐비닛 크기는 거의 같으며 시안의 원근 차이만 허용한다.
- CSS Grid는 6열 논리 그리드를 권장한다.
  - 1번: 2–4열.
  - 2번: 4–6열.
  - 3번: 1–3열.
  - 4번: 3–5열.
  - 5번: 5–7열.
- 각 TV는 둥근 직사각형의 은색 크롬 캐비닛, 안쪽 검은 베젤, 볼록한 짙은 화면, 작은 청백색 전원등, 원형 노브 2개를 갖는다.
- 크롬은 단일 회색이 아니라 흰 하이라이트 → 밝은 은색 → 중간 회색 → 짙은 회색 → 흰 반사광의 다층 그라데이션으로 만든다.
- 캐비닛에는 바깥 그림자와 여러 겹의 inset 그림자를 사용해 두께와 반사를 만든다.
- 화면에는 미세한 수평 주사선, 가장자리 비네트, 약한 푸른 내부 발광을 넣는다.

### 안테나와 동물 귀

- 1번 TV 상단 정중앙에 하나의 크롬 소켓이 있다.
- 흰 천사 날개와 검은 악마 날개가 그 하나의 소켓에서 동시에 시작해 V자로 뻗는다.
- 두 날개의 시작점이 벌어지거나 별도 소켓처럼 보이면 실패다.
- 흰 날개는 왼쪽 위, 검은 박쥐 날개는 오른쪽 위로 열린다.
- 3번 TV에는 작은 강아지 귀 2개가 있다.
  - 둥글고 살짝 아래로 접힌 형태.
  - 좌우 대칭이며 TV 상단 양 끝 쪽에 붙는다.
- 5번 TV에는 작은 고양이 귀 2개가 있다.
  - 뾰족한 삼각형 형태.
  - TV 상단 좌우 끝에 같은 크기와 각도로 정확히 대칭 배치한다.
  - 뿔처럼 중앙으로 몰리지 않게 한다.

### 외부 날개와 꼬리

- 3번 TV 왼쪽 뒤에서 큰 흰 천사 날개가 화면 왼쪽으로 뻗는다.
- 흰 날개는 여러 겹의 깃털과 부드러운 음영이 보이되 TV 클릭 영역을 가리지 않는다.
- 5번 TV 오른쪽 뒤에서 검은 악마 꼬리가 오른쪽 위로 휘어진다.
- 꼬리 끝은 작은 검은 화살촉 형태다.
- 장식은 `pointer-events: none`, `aria-hidden="true"`로 처리한다.

### 다섯 화면의 아이콘

화면 내부에는 아래 아이콘 하나만 중앙에 둔다.

| 번호 | 아이콘 | 역할 |
|---|---|---|
| 1 | 음양 심볼 | 시즌/포트폴리오 진입 |
| 2 | 음소거된 스피커 | BGM 켜기/끄기 |
| 3 | 흰 깃털 날개 | YANG 룩 진입 |
| 4 | `(—)` 형태의 미니멀 얼굴 | 브랜드 소개 진입 |
| 5 | 박쥐/악마 날개 | YIN 룩 진입 |

- 아이콘은 실제 inline SVG 또는 CSS 도형으로 구현한다.
- 아이콘은 선명한 흰색과 아주 옅은 청백색 발광을 사용한다.
- 화면 안에는 `2026 F/W`, `BGM OFF`, `YANG LOOK`, `ABOUT ECHO`, `YIN LOOK`, 화살표, 제품명, 로딩 문구를 시각적으로 표시하지 않는다.
- 의미와 상태는 `aria-label`, `aria-pressed`, `.sr-only` 텍스트로 제공한다.

### 하단

- 왼쪽 아래에 작고 자간이 넓은 저작권 문구.
- 중앙 아래에 짧은 브랜드 문장과 작은 구분선.
- 오른쪽 아래에 작은 밑줄형 `ADMIN` 링크.
- 공개 사이트의 `ADMIN` 링크는 `/admin`을 사용해 현재 Worker의 관리자 리다이렉트 구조를 유지한다.

## 4. 기존 아키텍처와 연결 규칙

### 유지할 데이터 흐름

- 실제 제품 정본: `products.json`.
- 제품 로딩과 안전 처리: `catalog.js`의 `ProductCatalog`.
- 제품 목록: `products.html`.
- 제품 상세: `product.html?id=<id>`.
- 포트폴리오 정본: `portfolio.json`.
- 포트폴리오 렌더링: `portfolio.js`와 `#portfolioContent`.
- 관리자: 배포 환경의 `/admin` → 통합 관리자 주소 리다이렉트.
- `scripts/capsule-data.js`는 홈 프레젠테이션 데이터이며 제품 정본을 대체하지 않는다.

### TV별 경로

- 1번 음양 TV: `portfolio.html`.
- 2번 스피커 TV: 링크가 아니라 `<button type="button">`.
- 3번 YANG TV: `products.html?polarity=YANG`.
- 4번 얼굴 TV: `about.html`.
- 5번 YIN TV: `products.html?polarity=YIN`.
- `ALL LOOKS`: `products.html`.
- `CONTACT`: `contact.html`.
- `ADMIN`: `/admin`.

### 음·양 제품 연결

- 관리자 데이터 스키마를 바꾸지 않는다.
- `products.json`에 새 `polarity` 필드를 강제로 넣지 않는다.
- 기존 `scripts/capsule-data.js`의 `productName`과 `polarity`를 프레젠테이션 매핑으로 사용하고, `ProductCatalog.loadVisibleProducts()`가 반환한 실제 제품과 기존의 정확 이름/featured/slot 폴백 규칙으로 연결한다.
- `products.html?polarity=YANG|YIN`을 구현할 때도 운영 제품 데이터를 하드코딩하지 않는다.
- 알 수 없는 값 또는 매핑되지 않은 제품은 숨기지 말고 전체 목록 폴백과 사용자에게 보이는 짧은 안내를 사용한다.
- 이 필터 기능이 이번 메인 작업 범위를 넘어가면 URL 계약과 테스트를 먼저 만들고, 다음 기능 커밋에서 제품 목록 필터를 완성한다.

### BGM 연결

- 저장소에 승인된 음원 파일이 있는지 먼저 검색한다.
- 음원 파일이 없으면 임의 음악, 원격 음원 URL, 자동재생을 추가하지 않는다.
- 음원이 없을 때도 2번 TV의 음소거 아이콘과 접근 가능한 버튼 구조는 완성한다. 버튼은 안전한 OFF 상태를 유지하고 404 요청을 만들지 않는다.
- 음원이 제공된 경우에만 같은 출처의 로컬 파일을 연결한다.
- 초기 상태는 OFF다. 사용자 클릭 후에만 재생한다.
- 재생 상태는 `aria-pressed`, `aria-label`, 화면 아이콘으로 함께 반영한다.
- 페이지 이동과 `pagehide`에서 오디오와 이벤트를 정리한다.
- 재생 실패는 화면을 깨뜨리지 않고 OFF 상태로 되돌린다.

### 수정 금지

아래 파일은 읽어서 연동을 이해할 수 있지만 수정하지 않는다.

- `.github/**`
- `AGENTS.md`
- `STUDENT_GUIDE.md`
- `RULES.md`
- `.assetsignore`
- `wrangler.jsonc`
- `wrangler.admin.jsonc`
- `worker.js`
- `products.json`
- `portfolio.json`
- `admin-config.json`
- `brand-config.js`
- `admin.html`
- `admin.css`
- `admin.js`
- `catalog.js`의 보안 함수
- `portfolio.js`의 데이터 로딩

## 5. 커밋 계획과 성취 조건

모든 커밋 제목은 `[학생이름] 작업 요약` 형식을 사용한다. 학생 이름은 `git config --local --get progen.student-name`에서 읽는다. 각 단계 시작 전에 테스트 또는 검증 기준을 먼저 작성하고 실패를 확인한다. 구현 후 GREEN이 되면 리팩터링하고 커밋한다.

### 커밋 1 — 기준 시안과 홈 계약 고정

권장 제목: `[학생이름] 홈 Y2K 시안과 동작 계약 고정`

작업:

- 최종 시안을 구현 참고 자산으로 보존한다. 실제 페이지에서 이 전체 이미지를 불러오면 안 된다.
- `tests/home-contract.test.mjs`처럼 외부 패키지가 필요 없는 Node 테스트를 만든다.
- 테스트가 확인할 계약:
  - TV 역할 5개가 정확히 한 번씩 존재한다.
  - 1·3·4·5번은 예상 경로를 가진 링크다.
  - 2번은 BGM 버튼이며 `aria-pressed="false"` 초기값을 가진다.
  - 각 화면의 가시 콘텐츠는 아이콘 1개뿐이다.
  - 화면 내부 메타 텍스트 클래스가 없다.
  - 장식은 접근성 트리와 포인터 입력에서 제외된다.
  - `/admin`, `products.html`, `about.html`, `portfolio.html`, `contact.html` 경로가 유지된다.
- 구현 전 기존 HTML에 대해 테스트가 실패하는 것을 확인하고 실패 이유를 기록한다.

성취 조건:

- `node --test tests/home-contract.test.mjs`가 현재 구조의 불일치를 정확히 실패로 보여준다.
- 테스트가 없는 요소를 억지로 문자열 정규식에 맞추기 위해 마크업을 왜곡하지 않는다.
- 현재 작업자의 미커밋 변경이 보존돼 있다.

### 커밋 2 — 밝은 무대와 공통 디자인 토큰

권장 제목: `[학생이름] 크롬 Y2K 무대와 디자인 토큰 적용`

작업:

- `style.css`의 `:root`에 아이스 배경, 잉크, 스크린, 크롬, 청백색 발광 토큰을 정의한다.
- 기존 어두운 스튜디오 홈을 밝은 아이스 스튜디오로 전환한다.
- 헤더, 메인 타이포, 유광 바닥, 반사 레이어의 HTML/CSS 구조를 만든다.
- 공통 페이지 토큰은 이후 공개 페이지에도 재사용할 수 있도록 이름을 일반화한다.
- 기존 로고 자산과 브랜드 문구를 우선 사용한다.

성취 조건:

- 1584×1000에서 헤더, 좌우 타이포, 바닥선의 위치가 최종 시안과 맞는다.
- 페이지 전체에 가로 스크롤이 없다.
- 색이 어두운 기존 스튜디오로 남은 영역이 없다.
- 본문과 링크의 키보드 포커스가 밝은 배경에서 명확하다.
- HTML/CSS/JS 외 런타임 의존성이 추가되지 않는다.

### 커밋 3 — 2×3 구조의 크롬 CRT 5대

권장 제목: `[학생이름] 중앙 대칭 5대 CRT 구조 구현`

작업:

- `index.html`을 정확히 5개의 의미 있는 TV 요소로 재구성한다.
- 위 2대, 아래 3대의 6열 논리 Grid를 만든다.
- 캐비닛, 베젤, 볼록 화면, 노브, 전원등, 반사를 CSS 레이어와 pseudo-element로 만든다.
- TV 전체 링크 또는 버튼이 클릭 영역이 되게 한다.
- 클릭 가능한 TV에 hover, focus-visible, active 상태를 넣되 정지 상태의 시안 형태는 흐트러뜨리지 않는다.

성취 조건:

- 정확히 5대이며 추가 장식이 TV로 오인되지 않는다.
- 기준 화면에서 중심축과 좌우 외곽 거리가 시각적으로 대칭이다.
- 상단 2대의 중앙과 하단 3대의 중앙이 같은 x축에 있다.
- TV 사이 틈, 캐비닛 폭, 화면 폭, 노브 위치가 반복 규칙을 따른다.
- Chrome 1584×1000 스크린샷을 최종 시안과 50% 투명도 오버레이했을 때 주요 외곽선 오차가 4px 이내다.
- `node --test tests/home-contract.test.mjs`가 GREEN이다.

### 커밋 4 — 날개 안테나·강아지 귀·고양이 귀·꼬리

권장 제목: `[학생이름] TV 캐릭터 장식과 대칭 완성`

작업:

- 1번 TV 정중앙의 단일 소켓에서 흰 날개와 검은 날개가 같이 시작하도록 만든다.
- 3번 TV 양 끝에 둥글고 접힌 강아지 귀를 대칭 배치한다.
- 5번 TV 양 끝에 뾰족한 고양이 귀를 대칭 배치한다.
- 3번 TV 뒤의 큰 흰 날개와 5번 TV 뒤의 검은 꼬리를 추가한다.
- 모든 장식은 별도 요소 또는 pseudo-element이며 `aria-hidden`과 `pointer-events:none`을 적용한다.

성취 조건:

- 안테나의 두 날개 시작점은 픽셀 오버레이에서 하나의 소켓으로 보인다.
- 안테나 소켓 중심과 1번 TV 캐비닛 중심의 x 오차가 2px 이하다.
- 3번 귀는 강아지 귀, 5번 귀는 고양이 귀로 즉시 구분된다.
- 5번 고양이 귀의 좌우 크기·높이·끝단 여백 차이가 2px 이하다.
- 장식이 어떤 TV의 포커스나 클릭도 가로막지 않는다.

### 커밋 5 — 아이콘 전용 화면과 TV 동작

권장 제목: `[학생이름] 아이콘 TV 내비게이션과 BGM 상태 구현`

RED:

- 각 TV 역할의 경로와 BGM 상태 전이를 먼저 테스트한다.
- 정상: OFF 클릭 → ON, ON 클릭 → OFF.
- 경계: 음원 미제공, 재생 Promise 거절, 빠른 연속 클릭.
- 접근성: `aria-pressed`와 `aria-label`이 실제 상태와 일치.
- 라우팅: YIN/YANG 쿼리 대소문자와 알 수 없는 값의 폴백.

GREEN:

- 다섯 화면에 inline SVG 아이콘 하나씩만 넣는다.
- 1·3·4·5번 경로를 연결한다.
- 2번 TV에 BGM 상태 로직을 연결한다.
- 기존 `EchoMotion`, 페이지 전환, `pageshow/pagehide` 구조와 충돌하지 않게 한다.
- 제품 정본을 하드코딩하지 않는다.

성취 조건:

- 모든 화면에서 시각적으로 보이는 것은 아이콘 하나뿐이다.
- 화면 텍스트는 DOM에도 가시 텍스트로 존재하지 않는다.
- 마우스, Enter, Space로 모든 기능을 사용할 수 있다.
- 음원이 없을 때 네트워크 404와 콘솔 오류가 없다.
- `prefers-reduced-motion: reduce`에서는 튜닝/확대 전환이 즉시 또는 최소 동작으로 끝난다.
- 뒤로가기로 돌아왔을 때 `aria-busy`, 전환 overlay, 오디오 상태가 꼬이지 않는다.
- 모든 Node 테스트와 `node --check`가 통과한다.

### 커밋 6 — 반응형·모바일·접근성 완성

권장 제목: `[학생이름] 홈 반응형과 접근성 완성`

작업:

- 1584×1000을 기준으로 미세 조정한다.
- 1440×900, 1024×768, 768×1024, 390×844, 360×800에서 확인한다.
- 데스크톱에서는 2-over-3 구조를 유지한다.
- 아주 좁은 모바일에서는 TV 크기를 읽을 수 있도록 2열 + 마지막 TV 중앙 또는 세로 흐름으로 재배치한다.
- 안전영역, 터치 크기, 키보드 순서, 스크린리더 이름을 검증한다.
- 장식으로 인한 가로 스크롤을 막는다.

성취 조건:

- 360px 너비에서도 가로 스크롤이 없다.
- 터치 대상은 최소 44×44px이다.
- DOM 순서는 1→2→3→4→5이며 Tab 순서도 같다.
- 장식과 반사는 읽기 순서에 나타나지 않는다.
- 초점 표시가 크롬/스크린 색과 충분히 구분된다.
- 200% 확대에서도 주요 링크와 TV가 겹쳐 사용할 수 없는 상태가 되지 않는다.
- 모바일 메뉴의 기존 열기/닫기/Escape 동작이 유지된다.

### 커밋 7 — 공통 헤더·푸터와 보조 페이지 문법

권장 제목: `[학생이름] 공개 페이지에 크롬 Y2K 문법 확장`

대상:

- `about.html`
- `products.html`
- `product.html`
- `portfolio.html`
- `contact.html`
- 공통 `style.css`, 필요 시 `script.js`

작업:

- 밝은 아이스 배경, 검은 에디토리얼 타이포, 크롬 테두리, 짙은 CRT형 미디어 프레임을 공통 문법으로 적용한다.
- 모든 페이지의 헤더와 푸터를 동일한 구조로 맞춘다.
- 제품 카드는 관리자가 넣은 실제 이미지와 텍스트를 그대로 소비하되, 크롬 TV/스크린 문법으로 표현한다.
- 제품 상세의 리치 콘텐츠, 포트폴리오의 `#portfolioContent`, 문의 링크의 실제 동작을 보존한다.
- 관리자 편집기 자체의 UI는 변경하지 않는다.

성취 조건:

- 모든 공개 페이지가 같은 색, 모서리, 타이포, 포커스, 버튼 문법을 사용한다.
- 제품 추가·수정 후 공개 목록과 상세가 코드 수정 없이 갱신된다.
- 포트폴리오 관리자 저장 내용이 그대로 렌더링된다.
- 안전한 구매 링크 처리와 리치 텍스트 sanitizing이 유지된다.
- 모든 내부 링크가 존재하는 파일 또는 `/admin`으로 연결된다.
- 모바일 메뉴와 360px 레이아웃이 전 페이지에서 정상이다.

### 커밋 8 — 최종 시각 회귀와 배포 전 검증

권장 제목: `[학생이름] Y2K 사이트 시각 회귀와 최종 검증`

작업:

- 기준 뷰포트 스크린샷을 새로 찍어 최종 시안과 오버레이 비교한다.
- 콘솔 오류, 404, 깨진 이미지, 중복 id, 잘못된 링크를 검사한다.
- 보호 파일 변경 여부를 검사한다.
- 공통 페이지와 관리자 데이터 연결을 확인한다.
- 테스트를 통과시키기 위한 임시 코드, debug 로그, 숨은 텍스트를 제거한다.

성취 조건:

- 최종 시안과 비교할 때 TV 배치, 안테나, 귀, 타이포, 바닥선, 좌우 장식이 동일한 인상이다.
- 기준 화면 주요 좌표 오차가 허용 범위 안이다.
- `node --test tests/*.test.mjs` 통과.
- 변경된 모든 `.js`에 `node --check` 통과.
- 저장소의 기존 사이트 검증 스크립트 통과.
- 보호 파일 변경 0개.
- 공개 페이지 네트워크 요청에 404가 없다.

## 6. Claude Code 전달용 마스터 프롬프트

아래 내용을 Claude Code에 그대로 전달한다.

---

당신은 ECHO ARCHIVE 정적 웹사이트의 구현 담당자다. 최종 크롬 Y2K 시안을 기준으로 메인 페이지를 먼저 완성하고, 같은 디자인 문법을 나머지 공개 페이지에 확장하라. 설명이나 계획만 제출하지 말고, 아래 단계 전체를 RED → GREEN → REFACTOR 순서로 실제 구현·검증·커밋하라.

### 최종 시안

반드시 다음 이미지를 먼저 열어 픽셀 단위로 관찰하라.

`C:\Users\hb061\.codex\generated_images\01a080f8-beaf-7801-9248-56516d20e246\exec-a8ce2c49-5261-4cfb-bb93-2f49e5af0b5a.png`

기준 환경은 Chrome, DPR 1, 1584×1000px이다. 이 이미지가 데스크톱 시각 구현의 단일 기준이다. 이미지 생성 과정에서 생긴 연도나 작은 문구 오타가 기존 브랜드 콘텐츠와 충돌하면 기존 사이트 콘텐츠를 사용하되, 위치·크기·타이포·색·재질·여백은 시안을 따른다.

### 시작 전에 반드시 수행할 것

1. `AGENTS.md`를 끝까지 읽고 그 지침을 따른다.
2. `git status --short --branch`, `git diff`, `git log --oneline --decorate -10`으로 현재 작업을 확인한다.
3. 기존 미커밋 변경을 절대 삭제하지 않는다. `reset --hard`, 강제 checkout, force push를 사용하지 않는다.
4. `git fetch --prune origin` 후 활성 학생 브랜치와 열린 main 대상 PR을 확인하고 AGENTS.md의 1인/협업 판단 절차를 따른다.
5. 학생 이름은 `git config --local --get progen.student-name`에서 읽고, 없을 때만 사용자에게 한 번 질문한다.
6. 다음 파일의 역할을 먼저 읽는다: `index.html`, `style.css`, `script.js`, `catalog.js`, `portfolio.js`, `scripts/capsule-data.js`, `scripts/home.js`, `scripts/motion.js`, `scripts/components/echo-tv-wall.js`, `scripts/components/signal-transition.js`, `products.html`, `product.html`, `about.html`, `portfolio.html`, `contact.html`.
7. 보호 파일과 운영 데이터는 수정하지 않는다: `.github/**`, `AGENTS.md`, `STUDENT_GUIDE.md`, `RULES.md`, `.assetsignore`, `wrangler*.jsonc`, `worker.js`, `products.json`, `portfolio.json`, `admin-config.json`, `brand-config.js`, `admin.html`, `admin.css`, `admin.js`, `catalog.js`의 보안 함수, `portfolio.js`의 데이터 로딩.
8. 최종 시안 전체를 배경 이미지로 사용하지 않는다. 실제 HTML 요소와 CSS로 조형하고, 상호작용은 JavaScript로 구현한다.
9. 공개 사이트 런타임에는 HTML, CSS, JavaScript만 사용한다. 프레임워크, 번들러, Tailwind, Bootstrap, 신규 런타임 패키지를 추가하지 않는다.

### 구현해야 하는 정확한 화면

- 배경은 밝은 아이스 그레이 스튜디오이며 아래쪽에 유광 반사가 있다.
- 좌측 상단에 ECHO ARCHIVE 로고와 작은 서브 문구, 우측 상단에 ALL LOOKS와 CONTACT가 있다.
- 왼쪽에 작아진 `TOUCH THE`, 오른쪽에 `SCREEN!`이 굵고 좁은 검정 이탤릭 타이포로 배치된다.
- 정확히 5대의 은색 CRT TV가 위 2대, 아래 3대로 중앙 대칭 배치된다.
- TV는 흰 하이라이트, 은색 중간톤, 짙은 가장자리, inset 그림자, 검은 베젤, 볼록 화면, 청백색 전원등, 원형 노브 2개를 가진다.
- 1번 TV 상단 정중앙의 단일 크롬 소켓에서 흰 천사 날개와 검은 악마 날개가 같은 시작점으로 V자 형태로 뻗는다.
- 3번 TV 상단 양쪽 끝에는 작고 둥글게 접힌 강아지 귀가 대칭으로 있다.
- 5번 TV 상단 양쪽 끝에는 작고 뾰족한 고양이 귀가 같은 크기·높이·각도로 대칭 배치된다.
- 3번 TV 왼쪽 뒤에는 큰 흰 천사 날개, 5번 TV 오른쪽 뒤에는 검은 악마 꼬리가 있다.
- 화면에는 아이콘만 표시한다.
  - 1번: 음양.
  - 2번: 음소거 스피커.
  - 3번: 흰 깃털 날개.
  - 4번: `(—)` 얼굴.
  - 5번: 박쥐/악마 날개.
- 화면 안의 글자, 숫자, 이름, 배지, 캡션, 코드, 화살표는 모두 금지한다.
- 하단에는 저작권, 중앙 브랜드 문장, 오른쪽 ADMIN 링크가 있다.

### 실제 기능 경로

- 1번 → `portfolio.html`.
- 2번 → BGM 토글 버튼, 초기 OFF, 자동재생 금지.
- 3번 → `products.html?polarity=YANG`.
- 4번 → `about.html`.
- 5번 → `products.html?polarity=YIN`.
- ALL LOOKS → `products.html`.
- CONTACT → `contact.html`.
- ADMIN → `/admin`.

관리자 데이터 구조를 바꾸지 마라. 제품은 항상 `ProductCatalog.loadVisibleProducts()`와 `products.json`에서 읽는다. 음·양 분류는 `scripts/capsule-data.js`의 프레젠테이션 매핑을 이용한다. 운영 제품을 HTML이나 JavaScript에 직접 하드코딩하지 마라. 포트폴리오는 `portfolio.json`과 `portfolio.js` 연결을 유지한다.

BGM 파일을 저장소에서 먼저 검색하라. 승인된 로컬 음원이 없으면 임의 음원이나 원격 URL을 추가하지 말고, 2번 TV를 접근 가능한 OFF 상태로 완성하라. 음원이 없다는 이유로 화면을 깨뜨리거나 404 요청을 발생시키지 마라.

### 단계별 TDD와 커밋

각 단계에서 다음 순서를 지켜라.

1. 요구사항을 검증하는 테스트 또는 명확한 브라우저 검증 항목을 먼저 작성한다.
2. 구현 전에 테스트가 요구사항 때문에 실패하는지 확인한다. import 실패만으로 RED를 대신하지 않는다.
3. 해당 단계만 통과시키는 최소 구현을 한다.
4. 전체 테스트와 브라우저 검증을 수행한다.
5. GREEN 상태에서 중복 CSS/JS만 정리한다.
6. 기능 단위로 한 커밋을 만든다.
7. 매 단계 후 커밋 해시, 변경 파일, RED 원인, GREEN 결과, 남은 위험을 기록한다.

실행 단계와 커밋 제목:

1. `[학생이름] 홈 Y2K 시안과 동작 계약 고정`
   - 외부 패키지 없는 Node 테스트를 작성한다.
   - TV 5개, 역할, 링크, BGM 버튼, 화면 내 텍스트 금지, 접근성 속성을 검증한다.

2. `[학생이름] 크롬 Y2K 무대와 디자인 토큰 적용`
   - 밝은 배경, 헤더, 좌우 타이포, 유광 바닥, 재사용 가능한 CSS 토큰을 구현한다.

3. `[학생이름] 중앙 대칭 5대 CRT 구조 구현`
   - 6열 논리 Grid로 2-over-3 TV 월을 만든다.
   - CSS 다층 그라데이션과 inset 그림자로 크롬 TV를 구현한다.

4. `[학생이름] TV 캐릭터 장식과 대칭 완성`
   - 단일 소켓 날개 안테나, 강아지 귀, 고양이 귀, 외부 날개, 꼬리를 구현한다.

5. `[학생이름] 아이콘 TV 내비게이션과 BGM 상태 구현`
   - 아이콘 전용 화면, 경로, BGM 상태, 오류 폴백, reduced-motion을 구현한다.

6. `[학생이름] 홈 반응형과 접근성 완성`
   - 1584×1000 시안 정합과 360–1440px 반응형, 키보드, 스크린리더, 터치 크기를 완성한다.

7. `[학생이름] 공개 페이지에 크롬 Y2K 문법 확장`
   - About, Products, Product detail, Portfolio, Contact에 공통 문법을 적용한다.
   - 운영 데이터 로딩과 관리자 연결은 그대로 둔다.

8. `[학생이름] Y2K 사이트 시각 회귀와 최종 검증`
   - 모든 화면, 링크, 콘솔, 404, 보호 파일, 모바일, 관리자 데이터 연결을 검증한다.

### 테스트 상세

- 새 테스트는 `tests/*.test.mjs`에 두고 Node 내장 `node:test`와 `node:assert/strict`를 사용한다.
- 테스트를 위해 런타임 프레임워크나 새 패키지를 설치하지 않는다.
- 문자열 존재만 확인하는 테스트에 머무르지 말고, 가능한 동작 로직은 순수 함수로 분리해 직접 검증한다.
- 한 테스트는 한 동작만 검증하고 한국어 문장으로 이름을 작성한다.
- 정상, 경계, 실패 경로를 나눈다.
- CSS 픽셀 정합은 억지 단위 테스트 대신 지정 뷰포트의 스크린샷 오버레이로 검증한다.
- 반드시 확인할 뷰포트: 1584×1000, 1440×900, 1024×768, 768×1024, 390×844, 360×800.
- 기준 화면에서 TV/타이포/장식 주요 외곽선은 오버레이 오차 4px 이내를 목표로 한다.
- 안테나 소켓과 1번 TV 중심축 오차는 2px 이하, 고양이 귀 좌우 위치 오차는 2px 이하로 맞춘다.
- 360px에서 가로 스크롤이 없어야 하고, 모든 클릭 요소는 최소 44×44px이어야 한다.
- DOM 및 Tab 순서는 TV 1→2→3→4→5여야 한다.
- 스크린리더는 각 TV의 역할과 BGM 상태를 읽어야 하지만 시각 화면에는 아이콘 외 텍스트가 없어야 한다.

### 시각 구현 방법

- `.home-stage`는 최소 100svh의 밝은 스튜디오다.
- TV 조형물은 폭 `min()`/`clamp()`와 CSS Grid로 스케일하며, 1584×1000에서 기준 좌표를 먼저 맞춘다.
- TV 캐비닛은 최소 3개의 크롬 그라데이션과 바깥/안쪽 그림자를 조합한다.
- 볼록 화면은 border-radius, radial-gradient, inset shadow, scanline pseudo-element로 만든다.
- 화면 반사는 opacity가 낮은 pseudo-element로 만들고 아이콘 가독성을 해치지 않는다.
- 복잡한 장식은 재사용 가능한 SVG/투명 자산으로 분리하고 `images/` 아래에 둔다.
- 로고·텍스트·아이콘을 전체 시안 이미지에서 잘라 쓰지 않는다.
- `prefers-reduced-motion`에서는 모든 반복 노이즈와 확대 이동을 제거한다.
- hover 효과는 TV를 약간 들어 올리고 청백색 광을 강화하는 정도로 제한한다.
- 화면 내부 텍스트는 CSS로 숨기는 방식이 아니라 마크업에서 제거한다. 접근성 이름은 화면 밖 속성으로 제공한다.

### 다른 공개 페이지의 디자인 문법

- 배경과 헤더/푸터는 메인과 같은 아이스 그레이·검정 잉크·크롬 포인트를 사용한다.
- 페이지 제목은 메인의 굵고 좁은 검정 타이포 문법을 사용한다.
- 제품 카드는 은색 캐비닛과 짙은 화면의 대비를 차용하되 실제 상품 사진과 관리자 텍스트가 읽히게 한다.
- 상세페이지의 상품 이미지와 리치 콘텐츠는 과도한 TV 장식으로 가리지 않는다.
- 포트폴리오의 `#portfolioContent`와 `portfolio.js`를 유지한다.
- 문의 링크, 이메일, 인스타그램, 구매 링크의 실제 href와 보안 속성을 유지한다.
- 공통 헤더와 푸터는 모든 공개 페이지에서 동일한 구조와 모바일 동작을 가진다.

### 완료 전에 반드시 실행할 검증

1. `node --test tests/*.test.mjs`
2. 변경한 모든 JavaScript에 `node --check`
3. 저장소의 기존 `.github/scripts/validate_site.py` 검증. 이 파일 자체는 수정하지 않는다.
4. 기준 뷰포트 6종 브라우저 확인.
5. 브라우저 콘솔 error 0건.
6. 로컬 asset 404 0건.
7. 키보드만으로 모든 TV와 메뉴 사용 가능.
8. `prefers-reduced-motion` 확인.
9. `/admin`, 제품 목록/상세, 포트폴리오 데이터 연결 확인.
10. `git diff --name-only`에서 보호 파일 변경 0개 확인.

### 중단 조건

- 기존 작업과 같은 줄에서 의도가 충돌해 두 변경을 함께 살릴 근거가 없을 때만 중단하고 질문한다.
- 승인된 BGM 파일이 없으면 전체 작업을 중단하지 않는다. OFF 상태의 버튼까지 완성하고 필요한 파일명과 연결 위치를 보고한다.
- 시각 자산을 새로 만들어야 하면 기존 자산을 먼저 재사용하고, 브랜드 정체성을 바꾸는 임의 이미지는 만들지 않는다.

### 최종 보고 형식

- 구현 결과 한 문단.
- 기능별 커밋 해시와 제목.
- 변경 파일.
- 실행한 테스트와 실제 통과 결과.
- 1584×1000 시안 비교 결과와 나머지 뷰포트 결과.
- 관리자/상품/포트폴리오 연결 확인 결과.
- 제공되지 않아 OFF 폴백으로 남긴 BGM 등 남은 항목.
- 보호 파일이 변경되지 않았다는 확인.

---

## 7. 구현 검토자가 특히 볼 것

- 이미지처럼 보이기만 하는 정적 배경인지, 실제 링크·버튼인지.
- 5대 TV의 수와 순서가 정확한지.
- 1번 날개 안테나가 하나의 소켓에서 시작하며 TV 중앙인지.
- 3번 강아지 귀와 5번 고양이 귀가 명확하고 대칭인지.
- 화면 안에 숨지 않은 가시 텍스트가 남아 있지 않은지.
- 관리자 데이터가 제품과 포트폴리오에 계속 반영되는지.
- BGM이 사용자 동작 없이 자동 재생되지 않는지.
- 전체 이미지 배경, 프레임워크, 새 데이터 스키마 같은 지름길을 사용하지 않았는지.
- 기준 데스크톱 시안과 모바일 UX를 모두 충족했는지.
- 기존 사용자의 변경과 보호 파일이 보존됐는지.
